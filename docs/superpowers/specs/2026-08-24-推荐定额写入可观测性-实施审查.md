# 推荐定额 写入可观测性（A/U/S 批）· 实施审查 + 后续实施要求

> 文档性质：**实施审查意见 + 下一批实施依据**。第 1–4 节是对 `e0bbb81` 的审查，**第 5 节是用户已拍板、交 codex 执行的改动**。
>
> 审查对象：`e0bbb81 feat: 增强推荐定额写入日志与SF补充绑定`
>
> 对照文件：`2026-08-23-推荐定额写入可观测性-实施说明.md`
>
> 审查日期：2026-08-24。审查方式：源码核对 + 离线回归实跑 + 实机日志与 DLL 哈希只读取证；未改源码、未连业务库、未部署。
>
> **拍板（2026-08-24）**：4.1 跨单元按"摘掉单元校验、保留项目连接与身份比对"实施；4.2 SF 按"绑定前预检"实施；4.3 / 4.4 一并带上。执行细则见第 5 节。

---

## 1. 结论

**实施合格。** 日志（A 批）、取消前置确认与保持窗口可见（U 批）、SF 替换/补充（S 批）三项均按说明落地，离线 9/9 全绿，五处 DLL 哈希一致，实机日志已产出且格式自洽。

但实机同时暴露 **1 个真实缺陷**：**改造的原始目标「跨单元写入」仍然做不到**，只是拦截点从 L3 原生键盘链换成了预览上下文校验。另有 1 项交互缺口和 3 项日志小瑕疵。四项均已拍板修复，见第 5 节。

---

## 2. 实施核对：符合说明

| 项 | 结果 |
|---|---|
| A 批日志 | `begin` / `plan` / `ok` / `blocked` / `failed` / `rejected` 六类全部落地。`TemplateFillFeature.cs:1197-1345` 与 `TemplateFillPanel.cs:2294-2377` |
| U 批界面 | `Hide()` / `mainForm.Activate()` / `mainForm.BringToFront()` / `WaitAgentUiIdle(100)` / `RestoreSmartPanelAfterHostWrite()` 全部移除；前置确认框删除；结果确定窗口保留 |
| S 批 SF | `PromptSmartSfBindingChoice`（`TemplateFillPanel.cs:1972`）+ `MergePreviewTargetGroup`（`TemplateFillNameMatch.cs:1202`）按完整身份去重；补充后重置 `LearningFeedbackAttempted` 强制重新回流 |
| 返回文案 | 六处阻断文案一字未改，既有断言继续通过 |
| `AGENTS.md` | 新增日志规则、SF 替换/补充规则、`build.ps1 -BuildOnly` 输出目录约束；`:171` 改写为"点击写入不再弹确认框、不得隐藏或强制交还焦点" |
| 改动规模 | +242 / −52，跨 6 个文件 |

### 2.1 比说明做得更好的一处

说明原文要求 `blocked` 把**返回文案原样**写进日志。codex 改成 `(reasonCode, userMessage)` 双参，日志只记 `reason=invalid_quantity code=PY-415` 这类稳定原因码。

这个改动是对的：按原说明，`组件数量无效，整组未写入：<工程量全名>` 会把工程量名写进日志，**正好违反同一份说明第 1.6 条"不记工程量名"**。原说明自相矛盾，实施方发现并修正了。

### 2.2 离线回归：9/9 PASS

隔离目录 `artifacts/recoexpand-observability-sf-e0bbb81`（含 NPOI 依赖），被测 DLL 即部署版，Windows PowerShell 5 实跑：

```
Test-SmartFillManualEntrySafety            PASS  ← 含新用例「SF 补充保留原组件并加入唯一设备费目标」
Test-QuantityFormulaLearning               PASS
Test-SmartFill                             PASS
Test-SmartFillComponentConversion          PASS
Test-SmartFillLearningScopeUi              PASS
Test-SqlOnlyLearning                       PASS
Test-LearningDbDoubleWrite                 PASS
Test-SmartFillProjectConnectionLifecycle   PASS
Test-SmartFillPartitionAndEntryCandidates  PASS
```

### 2.3 DLL 哈希：五处一致 `6429320c…`

`RecoQuotaRecommend/bin`、2020 运行目录、2024 运行目录、构建产物 `recoexpand-observability-sf-e0bbb81`、发布包两份（`01-综合扩展功能包` 与 `90-后续更新文件/综合扩展更新`）。同事模拟目录未被触碰。

---

## 3. 实机证据

2024 运行目录 `RecoExpandPanel.log`，2026-08-24：6 组 `begin`/`plan`/`ok` + 2 条 `rejected`，无 `blocked`/`failed`（这两条路径尚未被实机触发）。

**日志立刻兑现了设计意图的一例**：

```
01:54:48 Smart fill apply begin: unit=_ZGS_09#46 entry=0719-03-02-02#6112 items=2 groups=1
01:54:48 Smart fill apply plan: L1=0 L2=2 sf=1 entries=0719-03-02-02:1, 0719-03-02-03:1
01:54:48 Smart fill apply ok:  unit=_ZGS_09#46 business=2 marker=2 undoRows=4 firstId=4242 lastId=4245 elapsedMs=138
```

一行 `plan` 就证明了 SF 自动改道到**同级设备购置费条目** `0719-03-02-03`，`marker=2` 与两个落点条目对得上。这是旧实现下必须查学习库才能推断的事实。

52 条一批耗时 1746ms。写入成功/被拒都有痕，08-22 审查"无法从插件日志判断实机结果"的问题已解除。

---

## 4. 发现

### 4.1 🔴 跨单元写入仍然做不到——拦截点换了，目标没达成

**实机现象**（用户 2026-08-24 复现，截图 + 日志双证据）：预览后在左侧树点击**另一个单元**下的条目再写入，弹出

> 项目或当前单元已切换，请重新预览

日志同步记录：

```
02:41:05 Smart fill apply rejected: 上下文不可写 项目或当前单元已切换，请重新预览
02:41:18 Smart fill apply rejected: 上下文不可写 项目或当前单元已切换，请重新预览
```

**根因（源码核对，两层）**：

**第一层：写入目标单元根本不来自树选中项。**

`TryResolveCurrentSmartEntry`（`TemplateFillPanel.cs:476-557`）解析出的条目**序号/编号/名称取自树选中节点**，但

```csharp
UnitId = selection.CurrentUnitId,     // TemplateFillPanel.cs:550
```

`selection` 来自 `CaptureAgentSelection(mainForm)`，即**宿主当前显示的单元**。所以 `entry.UnitId` 永远等于宿主当前单元，从来不是"树上点中的那个条目所属的单元"。用户点 B 单元的条目时，宿主会跟着切到 B，于是它其实变成了"同单元写入"，只是预览过期了。

**第二层：预览失效判定把"单元"算了进去。**

```csharp
// TemplateFillPanel.cs:2206-2213
if (!Object.ReferenceEquals(context.ProjectConnection, entry.ProjectConnection) ||
    !String.Equals(context.ProjectConnectionIdentity, entry.ProjectConnectionIdentity, ...) ||
    context.CurrentUnitId != entry.UnitId ||
    (!String.IsNullOrWhiteSpace(context.CurrentUnitCode) &&
     !String.Equals(context.CurrentUnitCode, entry.UnitCode, ...)))
{
    error = "项目或当前单元已切换，请重新预览";
    return false;
}
```

`context.CurrentUnitId` 是**建预览那一刻**宿主的当前单元（`:1263-1264` 捕获）。于是实际规则变成：**一次预览只能在建预览时的那个单元里写，换单元必须重新预览。**

**这个限制没有技术依据——预览内容与单元完全无关：**

| 证据 | 结论 |
|---|---|
| `SmartFillFeature.cs` 全文 `UnitId` 出现 **0 次** | 预览构建链路不读单元 |
| `LoadProjectQuotas`（`TemplateFillNameMatch.cs:1152-1183`）的 SQL 无 `总概算序号` 过滤 | 项目内定额身份是全项目口径，非按单元 |
| `context.CurrentUnitId` / `CurrentUnitCode` 在全仓**只被 `:2208-2210` 这一处读取** | 摘掉不影响其他逻辑 |
| 写入侧 `LoadSmartFillStructuralRow(conn, targetUnitSeq, itemSeq)`（`TemplateFillFeature.cs:1125`）本就按**目标单元**取结构行，三级兜底（目标条目 → 同单元 → 全表） | 换单元后仍能构造出正确业务行 |

且这与 2026-08-08 的拍板决定 2「**预览与左侧树选中完全解耦**」直接冲突——单元校验把二者又绑回去了。

**为什么项目连接与身份比对必须留下**（与单元不是一回事）：预览项携带的 `ChosenQuotaSeq`（定额序号）与 `ChosenItemSeq`（条目序号）是**某一个具体数据库里的主键**，写入时据此整行复制并定位条目。若换成另一个项目库，同样的主键指向完全无关的行，**SQL 全部执行成功但数据全错**，是静默污染。单元只是同库内 `定额输入` 的一个列值，写错看得见、可撤销。风险量级差着数量级。

### 4.2 🟡 补充 SF 后不重算 SF 双向约束，整组阻断要到点写入才暴露

`RefreshSmartSfEntryState()` 只在 `RefreshCurrentSmartEntry`（`TemplateFillPanel.cs:577`）里调用；右键绑定收尾走的 `RefreshTargetGroupInGrid`（`:1493`）只重画行、不重算 `SfEntryBlocked`。

于是在**没有唯一同级设备购置费条目**的条目上使用"补充"：

- 界面不标红、写入按钮照常可点、状态列只显示"已补充 SF 设备费（组 N 条）"；
- 点写入才收到「未找到唯一的同级设备购置费条目」（或「同级设备购置费条目不唯一」）——来自 `TryResolveSiblingEquipmentEntry`（`TemplateFillPanel.cs:700-753`），经 `StampSelectedSmartEntries:2255` 早退，连 `ApplyFillToSelectedEntry` 都没进；
- 而且是**整组**被挡——`IsNameQuotaGroupSafeForWrite`（`TemplateFillFeature.cs:1052-1057`）只要任一成员 `SfEntryBlocked` 即判整组不安全，**原本可写的普通定额被一起挡住**。

反方向同样存在：**当前条目本身就是设备购置费条目**时，组里的普通定额会触发「设备购置费条目只接受 SF，所选组件整组未写入」（`:2263`），补充 SF 同样白做。

不是数据风险（阻断方向正确，且有日志），是白做一次操作。

### 4.3 ⚪ 日志三处小瑕疵

| 项 | 位置 | 说明 |
|---|---|---|
| 事务失败连写两行 | `TemplateFillFeature.cs:1339-1345` | `catch` 内先 `Log(... failed ...)`，随后 `return blocked("transaction_failed", ...)` 又写一行。`failed` 已含异常类型与耗时，`blocked` 那行是冗余 |
| 写入异常归类为 `rejected` | `TemplateFillPanel.cs:2377` | `catch` 里记 `Smart fill apply rejected: exception …`，语义上它是失败不是界面拒绝，归 `failed` 更准（前缀相同，不影响 grep） |
| `blocked` 不带耗时 | `blocked` 委托 | `applyWatch` 已经起了但阻断分支没用上，顺手加 `elapsedMs` 更一致 |

### 4.4 ⚪ 文档一句残留

`2026-08-23-推荐定额写入可观测性-实施说明.md:111` 仍写着「逐条补 `rejected` / `canceled`」，而 `canceled` 已随前置确认框取消一并作废（该文抬头 `:13` 与事件表都已改对，`AGENTS.md` 新规则也只列六类）。删半句即可。

---

## 5. 实施要求（用户已拍板，交 codex 执行）

### 5.1 摘掉预览失效判定里的单元校验

**改 `IsSmartPreviewContextCurrent`（`TemplateFillPanel.cs:2206-2213`）**：

```csharp
if (!Object.ReferenceEquals(context.ProjectConnection, entry.ProjectConnection) ||
    !String.Equals(context.ProjectConnectionIdentity, entry.ProjectConnectionIdentity, StringComparison.OrdinalIgnoreCase))
{
    error = "项目已切换，请重新预览";
    return false;
}
```

- 删掉 `context.CurrentUnitId != entry.UnitId` 与 `CurrentUnitCode` 两个条件；
- 错误文案同步去掉"或当前单元"（现文案会误导用户去重新预览）；
- **两行连接判断一个都不能少**，理由见 4.1 末段。

**连带清理**：

| 位置 | 处理 |
|---|---|
| `SmartPreviewContext` 的 `CurrentUnitId` / `CurrentUnitCode`（`:84-85`） | 删除字段 |
| 建预览时的两处赋值（`:1263-1264`） | 删除 |
| `AgentSelectionSnapshot selection = CaptureAgentSelection(mainForm);`（`:1257`） | 该处 `selection` 删完就没有别的用途，一并删除（**注意不要误删 `:540` 那个同名局部变量**，`TryResolveCurrentSmartEntry` 仍在用） |

**不要动**：

- `TryResolveCurrentSmartEntry` 的 `UnitId = selection.CurrentUnitId`（`:550`）——写入目标单元继续取宿主当前单元；
- `IsSmartPreviewContextCurrent` 余下的失效条件（`PreviewVersion`、推荐范围、工作表、目标列、活动工作簿、软件分区、办法号）；
- 临写入前的 `RefreshCurrentSmartEntry(true)` + 条目身份二次核对（`AGENTS.md:171` 要求）。

**改完后的预期行为**：预览一次 → 在宿主里切到任意单元的条目 → 直接写，不必重新预览；`begin` / `ok` 日志的 `unit=` 如实记录每次落点。

**明确不在本批范围**：不引入"在推荐窗口内显式选择目标单元"（那是宿主停在 A、向 B 写的另一种交互，需另立一批）；不新增 (单元, 条目) 配对校验——`章节表` 没有单元列，条目序号本就全项目共享，无从校验，`IsOptionalSmartTreeSequenceConsistent` 的既有弱点仍按原样保留。

### 5.2 SF 双向约束提前到右键绑定时预检

**先抽出共用判定**，避免预检与写入两处判断漂移。把 `StampSelectedSmartEntries`（`TemplateFillPanel.cs:2245-2270`）中"解析同级设备购置费条目 + 两条组级判断"整段搬进新函数，**逻辑与文案一字不改**：

```csharp
// 写入与右键补充预检共用：判定这批目标在该条目下是否满足 SF 双向约束。
// 返回 false 时 error 为用户可见原因；返回 true 时 sf* 为 SF 目标应改道到的条目。
private static bool ValidateSmartSfEntryConstraint(SqlConnection conn, CurrentSmartEntry entry,
    IEnumerable<FillPreviewItem> items, out long sfSequence, out string sfCode, out string sfName, out string error)
```

- 函数体 = 现有 `:2247-2270` 的内容（`currentIsEquipment` 判定 → `needsSfRedirect` 时调 `TryResolveSiblingEquipmentEntry` → 按 `TargetRow` 分组的两条判断），**保持"整批只解析一次同级条目"的现状**，不要改成逐组查询；
- `StampSelectedSmartEntries` 改为：先调它，失败则原样返回 `error`；成功后再执行既有的 `ChosenItemSeq` / `ChosenItemNo` / `ChosenItemName` / `SfRedirect` / `EntrySource` 赋值循环。**赋值逻辑不改**。

**再加一个纯函数**，给离线测试留接缝（不碰数据库的那部分判断）：

```csharp
// 纯函数：""=通过，非空=阻断原因。文案必须与 :2263 / :2268 完全一致。
internal static string DescribeSmartSfEntryConflict(bool currentIsEquipment, bool hasSf, bool hasNonSf, bool siblingResolved)
```

`ValidateSmartSfEntryConstraint` 内部调用它得出组级结论。

**接入预检**（`OnBindSelectedQuotaToRow`，`TemplateFillPanel.cs:2141-2151`）：在弹 `PromptSmartSfBindingChoice` **之前**——

1. 只在 `smartOnly && currentEntryWritable && currentSmartEntry != null` 时预检；当前条目解析不出来就跳过预检、维持现状（不要因为预检失败而挡住绑定）；
2. 用 `MergePreviewTargetGroup(oldGroup, replacements)` 的结果作为 `items` 调 `ValidateSmartSfEntryConstraint`；
3. 失败时把 `error` 作为警告传给对话框，并**只给「替换 / 取消」**，不给「补充」。

**对话框改造**：`PromptSmartSfBindingChoice(int existingCount, int selectedCount, string blockWarning)`。`blockWarning` 非空时：

- 顶部多一行红字提示，例如「当前条目 `0309-01-03-06`：未找到唯一的同级设备购置费条目。补充 SF 后整组将无法写入。」；
- 隐藏或禁用「补充」按钮，`AcceptButton` 保持「替换」；
- 窗体高度相应加大，别把文字挤没。

**记一条日志**（沿用现有前缀）：`Smart fill sf append blocked: entry=<条目编号> <error>`，便于事后确认用户是在被告知的情况下改走替换的。

**不需要**再补 `RefreshSmartSfEntryState()`：预检把问题提前到绑定时，而用户在树上切换条目时 `RefreshCurrentSmartEntry` 本来就会重算 `SfEntryBlocked` 并标红，两条路径已经闭合。

### 5.3 日志三处清理

1. `TemplateFillFeature.cs:1339-1345`：`catch` 内保留 `failed` 一行，`return` 改为直接返回原文案，**不再走 `blocked(...)`**（避免同一次失败两条日志）；
2. `TemplateFillPanel.cs:2377`：`Smart fill apply rejected: exception …` 改为 `Smart fill apply failed: 界面异常 <类型>: <消息>`；
3. `blocked` 委托内补 `elapsedMs=` + `applyWatch.ElapsedMilliseconds`。

**六处阻断的用户可见返回文案仍然一字不许改**。

### 5.4 文档修正

删掉 `2026-08-23-推荐定额写入可观测性-实施说明.md:111` 中的「/ `canceled`」。

### 5.5 `AGENTS.md` 修订

**改 `:171`**（现要求"事务开始前重新核对当前 `SelectedNode`、条目身份、项目连接对象和 `DataSource|Database`"）：在其后补一句——

> 推荐定额预览的失效判定只看项目连接对象与 `DataSource|Database`，**不得把宿主当前单元并入**：预览内容与单元无关（`SmartFillFeature` 全文不读单元，`LoadProjectQuotas` 是全项目口径），写入目标单元在临写入前重新解析并记入 `Smart fill apply` 日志。预览一次后允许在宿主内切换单元继续写入。

**新增一条**（紧随 SF 规则 `:100` 之后）：

> SF 双向约束的判定必须由写入路径与右键「补充」预检共用同一函数，阻断文案不得分叉；右键补充 SF 前必须先按目标条目预检，预检不通过时只提供「替换 / 取消」。

### 5.6 验收

**离线**（隔离目录 + `RECO_EXPAND_DLL`，Windows PowerShell 5）：现有 9 个用例保持全绿。`Test-SmartFillManualEntrySafety.ps1` 新增：

- 源码标记：`IsSmartPreviewContextCurrent` 所在方法体内**不得**再出现 `CurrentUnitId` / `CurrentUnitCode`；**必须**仍含 `Object.ReferenceEquals(context.ProjectConnection` 与 `ProjectConnectionIdentity`；panel 必须含 `ValidateSmartSfEntryConstraint`、`DescribeSmartSfEntryConflict`、`blockWarning`；
- 反射用例 `DescribeSmartSfEntryConflict`：
  - 设备购置费条目 + 组内有非 SF → 返回 `设备购置费条目只接受 SF，所选组件整组未写入`；
  - 非设备购置费条目 + 组内有 SF + 同级未解析到 → 返回 `未找到唯一同级设备购置费条目，所选组件整组未写入`；
  - 非设备购置费条目 + 组内有 SF + 同级已解析 → 返回 `""`；
  - 组内无 SF 且条目非设备购置费 → 返回 `""`。

**实机**（用户操作，代理只读核对）：

1. **跨单元**：预览 → 在树上切到**另一个单元**的条目 → 写入成功，日志 `begin`/`ok` 的 `unit=` 为新单元，`plan` 的 `entries=` 为新条目；库内按 `总概算序号 + 条目序号` 核对，原单元未被误写；
2. **跨项目仍然被挡**：关闭项目、打开另一个项目 → 用旧预览写入 → 提示「项目已切换，请重新预览」，日志 `rejected`；
3. **SF 预检生效**：在没有唯一同级设备购置费条目的条目上，右键含 SF 的软件选中行 → 对话框直接显示原因且**没有「补充」**；
4. **反方向**：在设备购置费条目上、组内有普通定额时同样提前告知；
5. **正常场景不受影响**：有唯一同级设备购置费条目时「补充」照常可用，写入后 `plan` 行的 `entries=` 显示两个条目；
6. **日志**：`failed` 只出现一行，`blocked` 带 `elapsedMs`。

### 5.7 本批不要做的事

- 不引入窗口内显式选择目标单元；
- 不新增 (单元, 条目) 配对校验；
- 不改写入目标单元的来源、不改条目身份二次核对；
- 不改任何用户可见的阻断文案（含 SF 两条）；
- 不重开重复写入防护，不改预览行/勾选状态/滚动位置；
- 不动推荐定额以外的功能，不把工作树中与本批无关的改动纳入提交。

---

## 6. 已拍板、不再处理的项

以下均经用户确认，**不是待办，也不是缺陷单**，记录在此以免后续审查重复提出：

| 项 | 决定 |
|---|---|
| 重复写入防护 | **取消**（2026-08-23）。2026-08-24 01:54-01:56 三批相同写入（条目 `0719-03-02-02`，ID 段 4242-4245 / 4246-4249 / 4250-4253）经确认**不是误触**，无需清理 |
| 已有 SF 的组件被普通定额整组替换时 SF 静默丢弃 | **不做对称处理**。触发「替换/补充」只看新选中行是否含 SF（`TemplateFillPanel.cs:2143`），维持现状 |
| 取消前置确认后"单击即写" | **是预期交互** |
| 两处 `StampSelectedSmartEntries` 紧邻（`:2244`、`:2267`） | **保留**，不合并 |

---

## 7. 放行结论

`e0bbb81` 的实现质量与说明符合度良好，离线全绿，日志能力已在实机验证，**建议保留该提交**。第 5 节的四项改动作为紧随其后的一批——在 5.1 落地并通过实机第 1 项验证之前，「推荐定额可以写入任意单元」这句话仍然不能算数。
