# 模板铺量 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做一个"模板铺量"独立面板：把第一个单元里某专业（轨道/站场/桥梁…）已做好的定额连同与 Excel 工程量的对应关系固化为模板，再对用户指定的目标单元，按对应关系**追加**同样的定额并填入该单元工程量。

**Architecture:** 全部复用现有 `partial class FormPanel`（RecoNet）里的零件——读模板（join 章节表查 条目编号/定额编号/定额调整/工程数量输入）、读 Excel（NPOI/COM）、插定额（粘贴 编号+数量，diff 出新定额序号）、套定额调整、撤销栈。新增两个聚焦文件：后端逻辑 `TemplateFillFeature.cs` 与面板 UI `TemplateFillPanel.cs`，并在定额输入右键菜单挂入口。插入策略＝"粘贴定额编号(含*系数)+数量(保留计算式)→粘贴定额调整→其余主程序自动算"。

**Tech Stack:** C# .NET Framework 4.x，WinForms，NPOI（读 xls/xlsx），SqlClient（项目库 SQL Server），JavaScriptSerializer（模板 JSON）。编译/部署用 `RecoQuotaRecommend/build.ps1`。

**设计依据：** `docs/superpowers/specs/2026-06-27-模板铺量-design.md`

---

## 约定：验证方式（本仓库无自动化测试框架）

- **编译检查（每个写代码任务后必做）**：
  ```
  & "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"
  ```
  期望：输出编译成功、生成并部署 `RecoExpandPanel.dll`，无 `error CS` 字样。出现 `error CS****` 即编译失败，必须修。
- **手动冒烟（标注 [手测] 的步骤）**：打开 2020 软件项目，按步骤在界面操作并核对结果。无法自动化，作为该任务的验收。
- **提交粒度**：每个 Task 末尾提交一次。分支：先确认不在 `main` 直接提交——若在 `main`，先 `git switch -c feat/template-fill`。

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `tools/RecoExpandPanel/TemplateFillFeature.cs` | 后端：数据类型、模板生成、两种取数、预览构建、套用引擎、JSON 存取。`partial class FormPanel` 静态方法 | 新建 |
| `tools/RecoExpandPanel/TemplateFillPanel.cs` | 面板 UI（`TemplateFillPanel : Form`）：生成模板 / 选模式 / 选目标单元 / 取数 / 预览 / 执行 | 新建 |
| `tools/RecoExpandPanel/ExcelLinkFeature.cs` | 在 `AddExcelLinkItems` 增加右键项"打开模板铺量面板" | 修改 ~140 |

`build.ps1` 用通配 `tools/RecoExpandPanel/*.cs` 编译，新文件自动纳入，无需改构建脚本（Task 9 会核对）。

复用方法访问性：以上三处与 `AgentExecutor.cs`/`ExcelLinkFeature.cs` 同属 `namespace RecoNet` 的 `partial class FormPanel`，故新文件可直接调用其 `private static` 成员（`GetProjectConnection`、`LoadStore`、`TryReadXlsxCellValue`、`TryEvaluateWorkbookExpression`、`ExecuteAgentInsertGroup`、`BuildQuotaFieldUpdate`、`ExecuteAgentFieldUpdate`、`GetField<T>`、`Log` 等）。

---

## Task 1: 数据类型与 JSON 存取骨架

**Files:**
- Create: `tools/RecoExpandPanel/TemplateFillFeature.cs`

- [ ] **Step 1: 新建文件，写入命名空间、using、类型定义**

```csharp
using System;
using System.Collections.Generic;
using System.Data.SqlClient;
using System.Globalization;
using System.IO;
using System.Linq;
using System.Text;
using System.Web.Script.Serialization;
using System.Windows.Forms;

namespace RecoNet
{
    public partial class FormPanel : Form
    {
        // 取数模式
        public enum FillMode { ColumnAnchor = 1, FixedColumn = 2 }

        // 模板里的一条定额
        public sealed class FillTemplateRow
        {
            public string ItemNo;        // 条目编号，如 0401-01
            public string ItemName;      // 条目名称（显示/核对）
            public string QuotaCode;     // 定额编号，含 *系数 后缀，原样
            public string Adjust;        // 定额调整整串（可空）
            public int OrderInItem;      // 条目内序号，保持插入先后
            // 取数引用（来自源单元绑定）
            public string SourceSheet;   // 绑定时所在 sheet
            public string SourceExpr;    // 绑定表达式，如 "E5" 或 "E4+E5"；模式一套用时替换列/sheet
            public string SourceName;    // 源行项目名（供预览核对，读自源行名称列）
        }

        // 一份模板
        public sealed class FillTemplate
        {
            public string Name;
            public string Profession;        // 专业标签，如 "轨道"
            public string SourceUnitNo;      // 源单元 总概算编号，如 _ZGS_01
            public string WorkbookPath;      // 绑定用的工作簿
            public List<FillTemplateRow> Rows = new List<FillTemplateRow>();
        }

        // 预览/写入用的一条结果
        public sealed class FillPreviewItem
        {
            public bool Selected = true;
            public string ItemNo;
            public string QuotaCode;
            public string Adjust;
            public string SourceName;        // 源行项目名
            public string TargetName;        // 目标位置同行项目名（核对）
            public string QuantityText;      // 取到的数量（数字或计算式）
            public string Status;            // "" 正常；否则异常原因（红标）
            public int OrderInItem;
        }
    }
}
```

- [ ] **Step 2: 追加 JSON 存取方法（同一类内）**

在类内追加：

```csharp
        private static string TemplateFillDir()
        {
            string dir = Path.Combine(FindRecoQuotaDataDir(), "fill-templates");
            if (!Directory.Exists(dir)) Directory.CreateDirectory(dir);
            return dir;
        }

        private static void SaveFillTemplate(FillTemplate template)
        {
            JavaScriptSerializer serializer = new JavaScriptSerializer();
            serializer.MaxJsonLength = 1024 * 1024 * 16;
            string safe = String.Join("_", template.Name.Split(Path.GetInvalidFileNameChars()));
            string path = Path.Combine(TemplateFillDir(), safe + ".json");
            File.WriteAllText(path, serializer.Serialize(template), Encoding.UTF8);
        }

        private static List<string> ListFillTemplateNames()
        {
            return Directory.GetFiles(TemplateFillDir(), "*.json")
                .Select(p => Path.GetFileNameWithoutExtension(p))
                .OrderBy(n => n, StringComparer.OrdinalIgnoreCase)
                .ToList();
        }

        private static FillTemplate LoadFillTemplate(string name)
        {
            string path = Path.Combine(TemplateFillDir(), name + ".json");
            if (!File.Exists(path)) return null;
            JavaScriptSerializer serializer = new JavaScriptSerializer();
            serializer.MaxJsonLength = 1024 * 1024 * 16;
            return serializer.Deserialize<FillTemplate>(File.ReadAllText(path, Encoding.UTF8));
        }
```

> 复用 `FindRecoQuotaDataDir()`（已存在于 `ExcelLinkFeature.cs`，返回 RecoQuotaData 目录）。

- [ ] **Step 3: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 编译成功，无 `error CS`。

- [ ] **Step 4: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillFeature.cs
git commit -m "feat(模板铺量): 数据类型与模板JSON存取骨架"
```

---

## Task 2: 从源单元生成模板

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillFeature.cs`

源数据：`定额输入 DE join 章节表 ZJ on DE.条目序号=ZJ.条目序号`，按某专业的条目编号前缀过滤；取数引用从 `ExcelLinkStore`（按 `定额序号` 匹配）取 sheet/表达式。参照 `AgentExecutor.cs:521` 的列清单与 `ExcelLinkFeature.cs` 的 `LoadStore`/`ExcelQuotaLink`。

- [ ] **Step 1: 写模板生成方法**

在类内追加：

```csharp
        // 从某单元、某条目编号前缀（专业子树）生成模板。
        // itemNoPrefix 例："04"（轨道大类）或具体到 "0401"；用 like 前缀匹配条目编号。
        private static FillTemplate BuildFillTemplateFromUnit(
            Form mainForm, SqlConnection conn, string templateName, string profession,
            string unitNo, string itemNoPrefix)
        {
            FillTemplate template = new FillTemplate
            {
                Name = templateName, Profession = profession, SourceUnitNo = unitNo
            };

            // 取数引用：定额序号 -> (sheet, 表达式, 工作簿)
            ExcelLinkStore store = LoadStore(conn);
            Dictionary<long, ExcelQuotaLink> linkBySeq = new Dictionary<long, ExcelQuotaLink>();
            foreach (ExcelQuotaLink link in store.Links)
            {
                linkBySeq[link.QuotaSequence] = link;
            }

            using (SqlCommand cmd = conn.CreateCommand())
            {
                cmd.CommandText =
                    "select DE.定额序号, ZJ.条目编号, ZJ.条目名称, DE.定额编号, DE.工程数量输入, " +
                    "cast(DE.定额调整 as nvarchar(max)), DE.顺号, " +
                    "(select top 1 工程或费用项目名称 from 定额输入 d2 where d2.定额序号=DE.定额序号) as nm " +
                    "from 定额输入 DE inner join 章节表 ZJ on DE.条目序号=ZJ.条目序号 " +
                    "where DE.总概算编号=@unit and ZJ.条目编号 like @pfx " +
                    "order by ZJ.条目编号, DE.顺号";
                cmd.Parameters.AddWithValue("@unit", unitNo);
                cmd.Parameters.AddWithValue("@pfx", (itemNoPrefix ?? "") + "%");
                using (SqlDataReader reader = cmd.ExecuteReader())
                {
                    int order = 0;
                    string lastItem = null;
                    while (reader.Read())
                    {
                        long seq = Convert.ToInt64(reader.GetValue(0), CultureInfo.InvariantCulture);
                        string itemNo = Convert.ToString(reader.GetValue(1)).Trim();
                        if (itemNo != lastItem) { order = 0; lastItem = itemNo; }

                        FillTemplateRow row = new FillTemplateRow
                        {
                            ItemNo = itemNo,
                            ItemName = Convert.ToString(reader.GetValue(2)).Trim(),
                            QuotaCode = Convert.ToString(reader.GetValue(3)).Trim(),
                            Adjust = reader.IsDBNull(5) ? "" : Convert.ToString(reader.GetValue(5)).Trim(),
                            OrderInItem = order++,
                            SourceName = reader.IsDBNull(7) ? "" : Convert.ToString(reader.GetValue(7)).Trim()
                        };

                        ExcelQuotaLink link;
                        if (linkBySeq.TryGetValue(seq, out link))
                        {
                            row.SourceSheet = link.WorksheetName;
                            row.SourceExpr = String.IsNullOrEmpty(link.Expression) ? link.CellAddress : link.Expression;
                            if (template.WorkbookPath == null) template.WorkbookPath = link.ExcelPath;
                        }

                        template.Rows.Add(row);
                    }
                }
            }

            return template;
        }
```

> **已确认（用户）**：`定额输入` 用 `总概算编号` 定位单元（值形如 `_ZGS_01`），where 子句即 `DE.总概算编号=@unit`。其余列名 `条目名称`/`工程或费用项目名称`/`顺号` 以实际库为准，手测暴露即改。

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`。

- [ ] **Step 3: [手测] 列名核对**

单元定位列已确认为 `总概算编号`（值形如 `_ZGS_01`）。仅需在手测时（Task 8）顺带确认 `条目名称`/`工程或费用项目名称`/`顺号` 列存在；如某列名不符，按报错改正。
Expected: 查询无列名错误。

- [ ] **Step 4: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillFeature.cs
git commit -m "feat(模板铺量): 从源单元生成模板（条目/定额/调整/取数引用）"
```

---

## Task 3: 取数模式一（列锚点）

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillFeature.cs`

模式一：给定目标 sheet + 目标列字母，对每条模板行，把其 `SourceExpr` 里的**列替换为目标列、sheet 替换为目标 sheet**，再用现有 `TryEvaluateWorkbookExpression` 求值。复用 `ExcelLinkFeature.cs` 的 `TryEvaluateWorkbookExpression(workbookPath, sheet, expr, out display, out qty, out err)` 与 `ExtractCellAddressesFromExpression`、`ColumnNumberToName`、`TryParseCellAddress`。

- [ ] **Step 1: 写"列替换"与模式一取数**

```csharp
        // 把表达式里每个单元格的列字母替换为 targetColumn（行号不变）。
        // 例 "E5" + targetCol "F" -> "F5"；"E4+E5" -> "F4+F5"。
        private static string RetargetExprColumn(string expr, string targetColumn)
        {
            if (String.IsNullOrWhiteSpace(expr)) return expr;
            List<string> cells = ExtractCellAddressesFromExpression(expr);
            string result = expr.ToUpperInvariant();
            foreach (string cell in cells.OrderByDescending(c => c.Length))
            {
                CellRef cr;
                if (!TryParseCellAddress(cell, out cr)) continue;
                string replaced = targetColumn.ToUpperInvariant() + (cr.Row).ToString(CultureInfo.InvariantCulture);
                result = result.Replace(cell, replaced);
            }
            return result;
        }

        // 模式一：按目标 sheet+列，对每条模板行求工程量。
        private static List<FillPreviewItem> BuildPreview_ColumnAnchor(
            FillTemplate template, string targetSheet, string targetColumn)
        {
            List<FillPreviewItem> items = new List<FillPreviewItem>();
            foreach (FillTemplateRow row in template.Rows)
            {
                FillPreviewItem item = new FillPreviewItem
                {
                    ItemNo = row.ItemNo, QuotaCode = row.QuotaCode, Adjust = row.Adjust,
                    SourceName = row.SourceName, OrderInItem = row.OrderInItem
                };

                if (String.IsNullOrWhiteSpace(row.SourceExpr))
                {
                    item.Status = "模板未记录取数位置"; item.Selected = false; items.Add(item); continue;
                }

                string expr = RetargetExprColumn(row.SourceExpr, targetColumn);
                string display; decimal qty; string err;
                if (!TryEvaluateWorkbookExpression(template.WorkbookPath, targetSheet, expr, out display, out qty, out err))
                {
                    item.Status = "取数失败：" + err; item.Selected = false; items.Add(item); continue;
                }

                item.QuantityText = display;
                // 目标行同位置项目名（核对用），读名称列：取表达式首格的行、A~F 文本拼接
                item.TargetName = ReadRowNameAt(template.WorkbookPath, targetSheet, expr);
                if (qty == 0m) { item.Status = "数量为0"; }
                items.Add(item);
            }
            return items;
        }

        // 读某表达式首个单元格所在行的名称（A 到该格列前的非数字文本拼接），仅供人工核对。
        private static string ReadRowNameAt(string workbook, string sheet, string expr)
        {
            try
            {
                string first = ExtractFirstCellAddress(expr);
                CellRef cr;
                if (String.IsNullOrEmpty(first) || !TryParseCellAddress(first, out cr)) return "";
                List<string> parts = new List<string>();
                for (int col = 1; col < cr.Column; col++)
                {
                    string addr = ColumnNumberToName(col) + cr.Row.ToString(CultureInfo.InvariantCulture);
                    string val; string e;
                    if (TryReadXlsxCellValue(workbook, sheet, addr, out val, out e) && !String.IsNullOrWhiteSpace(val))
                    {
                        decimal d; string pe;
                        if (!TryEvaluateDecimal(val, out d, out pe)) parts.Add(val.Trim());
                    }
                }
                return String.Join(" ", parts.Take(6).ToArray()).Trim();
            }
            catch { return ""; }
        }
```

> 复用：`ExtractCellAddressesFromExpression`、`ExtractFirstCellAddress`、`TryParseCellAddress`、`ColumnNumberToName`、`TryEvaluateWorkbookExpression`、`TryReadXlsxCellValue`、`TryEvaluateDecimal`、`CellRef`（均在 `ExcelLinkFeature.cs`）。

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`。

- [ ] **Step 3: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillFeature.cs
git commit -m "feat(模板铺量): 取数模式一·列锚点"
```

---

## Task 4: 取数模式二（固定绑定列）

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillFeature.cs`

模式二：模板的 `SourceExpr`/`SourceSheet` 本身就是那一列固定单元格；用户已把目标单元数量粘进去。直接用模板原表达式（不替换列）按当前工作簿读值即可。

- [ ] **Step 1: 写模式二取数**

```csharp
        // 模式二：固定绑定列。直接读模板记录的原单元格（用户已把目标单元数量粘进该列）。
        private static List<FillPreviewItem> BuildPreview_FixedColumn(FillTemplate template)
        {
            List<FillPreviewItem> items = new List<FillPreviewItem>();
            foreach (FillTemplateRow row in template.Rows)
            {
                FillPreviewItem item = new FillPreviewItem
                {
                    ItemNo = row.ItemNo, QuotaCode = row.QuotaCode, Adjust = row.Adjust,
                    SourceName = row.SourceName, OrderInItem = row.OrderInItem
                };

                if (String.IsNullOrWhiteSpace(row.SourceExpr))
                {
                    item.Status = "模板未记录取数位置"; item.Selected = false; items.Add(item); continue;
                }

                string display; decimal qty; string err;
                if (!TryEvaluateWorkbookExpression(template.WorkbookPath, row.SourceSheet, row.SourceExpr, out display, out qty, out err))
                {
                    item.Status = "取数失败：" + err; item.Selected = false; items.Add(item); continue;
                }

                item.QuantityText = display;
                item.TargetName = ReadRowNameAt(template.WorkbookPath, row.SourceSheet, row.SourceExpr);
                if (qty == 0m) { item.Status = "数量为0"; }
                items.Add(item);
            }
            return items;
        }
```

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`。

- [ ] **Step 3: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillFeature.cs
git commit -m "feat(模板铺量): 取数模式二·固定绑定列"
```

---

## Task 5: 套用引擎（追加定额 + 套调整 + 撤销）

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillFeature.cs`

两阶段，纳入一个撤销记录：
- 阶段1：按条目分组，复用 `ExecuteAgentInsertGroup`（`AgentExecutor.cs:1859`）粘贴 `定额编号(含*系数) + 数量`，它会 diff 出新 `定额序号` 并写入 `undo.Rows`（Kind="I"）。
- 阶段2：对刚插入、且模板带 `Adjust` 的行，套定额调整。需把"新定额序号"和"模板行"对上——按条目内顺序配对（阶段1按条目粘贴顺序与模板顺序一致）。

参照 `ExecuteAgentInsertGroup` 返回的 `added` 顺序、`AgentInsertGroup`/`AgentQuotaInput` 结构、`BuildQuotaFieldUpdate`/`ExecuteAgentFieldUpdate`、撤销栈 `GetAgentUndoStack(mainForm)`、`AgentUndoRecord`/`AgentUndoRow`。

- [ ] **Step 1: 写套用引擎**

```csharp
        // 套用：把选中的预览项按条目分组追加到目标单元，套定额调整，登记撤销。
        private static string ApplyFill(Form mainForm, string targetUnitNo, List<FillPreviewItem> items)
        {
            List<FillPreviewItem> selected = items
                .Where(i => i.Selected && String.IsNullOrEmpty(i.Status))
                .OrderBy(i => i.ItemNo, StringComparer.OrdinalIgnoreCase)
                .ThenBy(i => i.OrderInItem)
                .ToList();
            if (selected.Count == 0) return "没有可写入的行。";

            using (SqlConnection conn = AgentCreateWorkConnection(mainForm))
            {
                AgentUndoRecord undo = new AgentUndoRecord { Summary = "模板铺量 -> 单元 " + targetUnitNo, Time = DateTime.Now };
                StringBuilder msg = new StringBuilder();
                int totalInserted = 0, totalAdjusted = 0;

                foreach (IGrouping<string, FillPreviewItem> g in selected.GroupBy(i => i.ItemNo))
                {
                    // 目标条目编号 = 该单元下与模板同编号的条目（已建好）。
                    string targetItemNo = ResolveTargetItemNo(conn, targetUnitNo, g.Key);
                    if (targetItemNo == null)
                    {
                        if (msg.Length > 0) msg.AppendLine();
                        msg.Append("条目 ").Append(g.Key).Append("：目标单元未找到对应条目，已跳过。");
                        continue;
                    }

                    List<FillPreviewItem> rows = g.OrderBy(i => i.OrderInItem).ToList();
                    AgentInsertGroup group = new AgentInsertGroup { ItemNo = targetItemNo };
                    foreach (FillPreviewItem r in rows)
                    {
                        group.Quotas.Add(new AgentQuotaInput { Code = r.QuotaCode, Quantity = r.QuantityText });
                    }

                    HashSet<long> before = LoadAgentItemQuotaIds(conn, targetItemNo);
                    ExecuteAgentInsertGroup(mainForm, conn, group, undo);
                    HashSet<long> after = LoadAgentItemQuotaIds(conn, targetItemNo);
                    List<long> added = after.Where(id => !before.Contains(id))
                                            .OrderBy(id => id).ToList();
                    totalInserted += added.Count;

                    // 阶段2：套定额调整（按追加顺序与 rows 配对；定额序号自增，故按 id 升序≈插入先后）
                    int n = Math.Min(added.Count, rows.Count);
                    for (int k = 0; k < n; k++)
                    {
                        string adj = rows[k].Adjust;
                        if (String.IsNullOrWhiteSpace(adj)) continue;
                        ApplyAdjustToQuota(conn, added[k], adj, undo);
                        totalAdjusted++;
                    }
                }

                if (undo.Rows.Count > 0)
                {
                    GetAgentUndoStack(mainForm).Add(undo);
                    GetAgentRedoStack(mainForm).Clear();
                }
                RefreshCurrentQuotaGrid(mainForm);

                msg.Insert(0, "已追加定额 " + totalInserted + " 条，套用调整 " + totalAdjusted + " 条。"
                    + (msg.Length > 0 ? Environment.NewLine : ""));
                return msg.ToString();
            }
        }

        // 目标单元下与模板同编号的条目。前提：条目已建好、编号一致 -> 直接返回原编号；
        // 若编号含单元前缀差异，按"同后缀"匹配（实施时按实际编号规则微调）。
        private static string ResolveTargetItemNo(SqlConnection conn, string targetUnitNo, string templateItemNo)
        {
            using (SqlCommand cmd = conn.CreateCommand())
            {
                cmd.CommandText = "select count(*) from 章节表 ZJ inner join 定额输入 DE on DE.条目序号=ZJ.条目序号 " +
                                  "where DE.总概算编号=@unit and ZJ.条目编号=@bh";
                cmd.Parameters.AddWithValue("@unit", targetUnitNo);
                cmd.Parameters.AddWithValue("@bh", templateItemNo);
                int c = Convert.ToInt32(cmd.ExecuteScalar(), CultureInfo.InvariantCulture);
                return c > 0 ? templateItemNo : null;
            }
        }

        // 用现有字段更新机制把定额调整整串写入某定额，并登记到撤销记录。
        private static void ApplyAdjustToQuota(SqlConnection conn, long quotaSeq, string adjust, AgentUndoRecord undo)
        {
            string oldAdj = null;
            using (SqlCommand cmd = conn.CreateCommand())
            {
                cmd.CommandText = "select cast(定额调整 as nvarchar(max)) from 定额输入 where 定额序号=@id";
                cmd.Parameters.AddWithValue("@id", quotaSeq);
                object o = cmd.ExecuteScalar();
                oldAdj = (o == null || o == DBNull.Value) ? "" : Convert.ToString(o);
            }
            using (SqlCommand cmd = conn.CreateCommand())
            {
                cmd.CommandText = "update 定额输入 set 定额调整=@adj where 定额序号=@id";
                cmd.Parameters.AddWithValue("@adj", (object)adjust ?? DBNull.Value);
                cmd.Parameters.AddWithValue("@id", quotaSeq);
                cmd.ExecuteNonQuery();
            }
            undo.Rows.Add(new AgentUndoRow
            {
                Kind = "F", QuotaSequence = quotaSeq, Table = "定额输入",
                KeyClause = "定额序号=@k0",
                KeyValues = new Dictionary<string, object> { { "@k0", quotaSeq } },
                OldValues = new Dictionary<string, object> { { "定额调整", (object)oldAdj } },
                NewValues = new Dictionary<string, object> { { "定额调整", (object)adjust } }
            });
        }
```

> 校对点（实施时按实际签名修正）：`AgentInsertGroup`/`AgentQuotaInput` 字段名（`ItemNo`/`Quotas`/`Code`/`Quantity`）、`AgentCreateWorkConnection`、`GetAgentUndoStack`/`GetAgentRedoStack`、`RefreshCurrentQuotaGrid`、`AgentUndoRow` 字段（`Kind`/`Table`/`KeyClause`/`KeyValues`/`OldValues`/`NewValues`）。这些都在 `AgentExecutor.cs`，签名以该文件为准。
> 阶段2 套调整后，主程序缓存的单价/合价可能需重算——若手测发现调整未生效到价格，改为复用 `BuildQuotaFieldUpdate`+`ExecuteAgentFieldUpdate`+`SyncAgentHostQuotaTable`（与设调整命令同路径），而非裸 update。

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`。

- [ ] **Step 3: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillFeature.cs
git commit -m "feat(模板铺量): 套用引擎（追加定额+套调整+撤销）"
```

---

## Task 6: 面板 UI（生成/取数/预览/执行）

**Files:**
- Create: `tools/RecoExpandPanel/TemplateFillPanel.cs`

参照 `ExcelSmartBindPanel`（`ExcelLinkFeature.cs:3827`）的构造与 `mainForm` 持有方式。面板为 `partial class FormPanel` 内的私有嵌套类还是独立类皆可；为聚焦，做独立 `internal sealed class TemplateFillPanel : Form`，通过 `FormPanel` 暴露的内部静态入口调用后端方法。

> 由于后端方法是 `FormPanel` 的 `private static`，面板要么作为 `FormPanel` 的嵌套类（可访问私有），要么 `FormPanel` 暴露 `internal static` 包装。**采用嵌套类**：把本面板类写进 `partial class FormPanel`，与 `ExcelSmartBindPanel` 一致。

- [ ] **Step 1: 写面板类（嵌套于 FormPanel）**

新建文件，结构如下（控件齐全、事件接后端）：

```csharp
using System;
using System.Collections.Generic;
using System.Data.SqlClient;
using System.Drawing;
using System.Linq;
using System.Windows.Forms;

namespace RecoNet
{
    public partial class FormPanel : Form
    {
        private sealed class TemplateFillPanel : Form
        {
            private readonly Form mainForm;
            private List<FillPreviewItem> preview = new List<FillPreviewItem>();

            private readonly ComboBox cmbTemplate = new ComboBox();
            private readonly TextBox txtUnit = new TextBox();        // 源单元/目标单元 总概算编号
            private readonly TextBox txtItemPrefix = new TextBox();  // 专业条目前缀
            private readonly TextBox txtName = new TextBox();        // 模板名
            private readonly Button btnBuild = new Button();
            private readonly ComboBox cmbMode = new ComboBox();      // 模式一/二
            private readonly TextBox txtSheet = new TextBox();       // 模式一目标 sheet
            private readonly TextBox txtColumn = new TextBox();      // 模式一目标列
            private readonly TextBox txtTargetUnit = new TextBox();  // 目标单元
            private readonly Button btnPreview = new Button();
            private readonly Button btnApply = new Button();
            private readonly DataGridView grid = new DataGridView();

            public TemplateFillPanel(Form owner)
            {
                mainForm = owner;
                Text = "模板铺量";
                StartPosition = FormStartPosition.CenterParent;
                ClientSize = new Size(900, 560);
                BuildLayout();
                ReloadTemplateList();
            }

            private void BuildLayout()
            {
                // —— 顶部：生成模板 ——
                AddLabel("源/目标单元号", 12, 15);
                txtUnit.SetBounds(110, 12, 90, 23); txtUnit.Text = "_ZGS_01";
                AddLabel("专业条目前缀", 210, 15);
                txtItemPrefix.SetBounds(300, 12, 80, 23); txtItemPrefix.Text = "04";
                AddLabel("模板名", 390, 15);
                txtName.SetBounds(440, 12, 130, 23); txtName.Text = "轨道-模板";
                btnBuild.SetBounds(580, 11, 110, 25); btnBuild.Text = "从该单元生成模板";
                btnBuild.Click += (s, e) => OnBuild();

                // —— 中部：套用配置 ——
                AddLabel("模板", 12, 50);
                cmbTemplate.SetBounds(60, 47, 200, 23); cmbTemplate.DropDownStyle = ComboBoxStyle.DropDownList;
                AddLabel("取数模式", 280, 50);
                cmbMode.SetBounds(350, 47, 150, 23); cmbMode.DropDownStyle = ComboBoxStyle.DropDownList;
                cmbMode.Items.AddRange(new object[] { "一·列锚点", "二·固定绑定列" });
                cmbMode.SelectedIndex = 0;
                AddLabel("目标sheet", 12, 82);
                txtSheet.SetBounds(80, 79, 120, 23); txtSheet.Text = "方案二";
                AddLabel("目标列", 210, 82);
                txtColumn.SetBounds(260, 79, 50, 23); txtColumn.Text = "E";
                AddLabel("目标单元", 330, 82);
                txtTargetUnit.SetBounds(400, 79, 90, 23); txtTargetUnit.Text = "_ZGS_02";
                btnPreview.SetBounds(510, 78, 80, 25); btnPreview.Text = "预览";
                btnPreview.Click += (s, e) => OnPreview();
                btnApply.SetBounds(600, 78, 100, 25); btnApply.Text = "写入目标单元";
                btnApply.Click += (s, e) => OnApply();

                // —— 预览表 ——
                grid.SetBounds(12, 115, 876, 430);
                grid.ReadOnly = false; grid.AllowUserToAddRows = false;
                grid.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill;
                grid.Columns.Add(new DataGridViewCheckBoxColumn { HeaderText = "选", Name = "sel", FillWeight = 6 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "条目", Name = "item", ReadOnly = true, FillWeight = 14 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "定额编号", Name = "code", ReadOnly = true, FillWeight = 16 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "调整", Name = "adj", ReadOnly = true, FillWeight = 14 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "源行项目名", Name = "sname", ReadOnly = true, FillWeight = 18 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "目标行项目名", Name = "tname", ReadOnly = true, FillWeight = 18 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "数量", Name = "qty", ReadOnly = true, FillWeight = 10 });
                grid.Columns.Add(new DataGridViewTextBoxColumn { HeaderText = "状态", Name = "st", ReadOnly = true, FillWeight = 14 });

                Controls.Add(txtUnit); Controls.Add(txtItemPrefix); Controls.Add(txtName); Controls.Add(btnBuild);
                Controls.Add(cmbTemplate); Controls.Add(cmbMode); Controls.Add(txtSheet); Controls.Add(txtColumn);
                Controls.Add(txtTargetUnit); Controls.Add(btnPreview); Controls.Add(btnApply); Controls.Add(grid);
            }

            private void AddLabel(string text, int x, int y)
            {
                Label l = new Label { Text = text, AutoSize = true };
                l.SetBounds(x, y, 60, 18); Controls.Add(l);
            }

            private void ReloadTemplateList()
            {
                cmbTemplate.Items.Clear();
                foreach (string n in ListFillTemplateNames()) cmbTemplate.Items.Add(n);
                if (cmbTemplate.Items.Count > 0) cmbTemplate.SelectedIndex = 0;
            }

            private void OnBuild()
            {
                try
                {
                    using (SqlConnection conn = GetProjectConnection(mainForm))
                    {
                        FillTemplate t = BuildFillTemplateFromUnit(mainForm, conn, txtName.Text.Trim(),
                            txtItemPrefix.Text.Trim(), txtUnit.Text.Trim(), txtItemPrefix.Text.Trim());
                        SaveFillTemplate(t);
                    }
                    ReloadTemplateList();
                    MessageBox.Show(this, "模板已生成并保存。", "模板铺量");
                }
                catch (Exception ex) { MessageBox.Show(this, "生成失败：" + ex.Message, "模板铺量"); }
            }

            private void OnPreview()
            {
                try
                {
                    FillTemplate t = LoadFillTemplate(Convert.ToString(cmbTemplate.SelectedItem));
                    if (t == null) { MessageBox.Show(this, "请先选择模板。", "模板铺量"); return; }
                    preview = cmbMode.SelectedIndex == 0
                        ? BuildPreview_ColumnAnchor(t, txtSheet.Text.Trim(), txtColumn.Text.Trim())
                        : BuildPreview_FixedColumn(t);
                    FillGrid();
                }
                catch (Exception ex) { MessageBox.Show(this, "预览失败：" + ex.Message, "模板铺量"); }
            }

            private void FillGrid()
            {
                grid.Rows.Clear();
                foreach (FillPreviewItem it in preview)
                {
                    int idx = grid.Rows.Add(it.Selected, it.ItemNo, it.QuotaCode, it.Adjust,
                        it.SourceName, it.TargetName, it.QuantityText, it.Status);
                    if (!String.IsNullOrEmpty(it.Status))
                        grid.Rows[idx].DefaultCellStyle.BackColor = Color.MistyRose;
                }
            }

            private void OnApply()
            {
                try
                {
                    // 回写勾选状态
                    for (int i = 0; i < preview.Count && i < grid.Rows.Count; i++)
                        preview[i].Selected = Convert.ToBoolean(grid.Rows[i].Cells["sel"].Value ?? false);

                    if (MessageBox.Show(this, "确认把勾选定额追加到单元 " + txtTargetUnit.Text.Trim() + "？",
                        "模板铺量", MessageBoxButtons.OKCancel) != DialogResult.OK) return;

                    string result = ApplyFill(mainForm, txtTargetUnit.Text.Trim(), preview);
                    MessageBox.Show(this, result, "模板铺量");
                }
                catch (Exception ex) { MessageBox.Show(this, "写入失败：" + ex.Message, "模板铺量"); }
            }
        }

        // 入口
        private static readonly Dictionary<Form, TemplateFillPanel> TemplateFillPanels = new Dictionary<Form, TemplateFillPanel>();
        private static void ShowTemplateFillPanel(Form mainForm)
        {
            TemplateFillPanel panel;
            if (!TemplateFillPanels.TryGetValue(mainForm, out panel) || panel == null || panel.IsDisposed)
            {
                panel = new TemplateFillPanel(mainForm);
                TemplateFillPanels[mainForm] = panel;
            }
            panel.Show(mainForm); panel.Activate();
        }
    }
}
```

> `OnBuild` 调 `BuildFillTemplateFromUnit` 的参数顺序按 Task 2 的签名核对（profession/unitNo/prefix）。`GetProjectConnection` 用于只读生成模板；`ApplyFill` 内部用 `AgentCreateWorkConnection`（写连接）。

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`。

- [ ] **Step 3: Commit**

```bash
git add tools/RecoExpandPanel/TemplateFillPanel.cs
git commit -m "feat(模板铺量): 面板UI（生成/取数/预览/执行）"
```

---

## Task 7: 右键菜单入口

**Files:**
- Modify: `tools/RecoExpandPanel/ExcelLinkFeature.cs`（`AddExcelLinkItems`，"打开Excel联动面板"之后）

- [ ] **Step 1: 在 openPanel 之后追加菜单项**

定位 `AddExcelLinkItems` 中 `openPanel` 插入完成处（`ExcelLinkFeature.cs:140-150` 一带），其后追加：

```csharp
                ToolStripMenuItem fillPanel = FindMenuItem(menu, "打开模板铺量面板");
                if (fillPanel == null)
                {
                    fillPanel = new ToolStripMenuItem("打开模板铺量面板");
                    fillPanel.Visible = true;
                    fillPanel.Available = true;
                    fillPanel.Enabled = true;
                    fillPanel.Click += delegate { ShowTemplateFillPanel(mainForm); };
                    menu.Items.Insert(Math.Min(baseIndex + 1, menu.Items.Count), fillPanel);
                }
```

> `baseIndex` 在该方法作用域内可用（见 105-150）。图标可选，先不加。

- [ ] **Step 2: 编译检查**

Run: `& "C:\Users\谢刚\Desktop\自动预算\RecoQuotaRecommend\build.ps1"`
Expected: 无 `error CS`，并部署成功。

- [ ] **Step 3: Commit**

```bash
git add tools/RecoExpandPanel/ExcelLinkFeature.cs
git commit -m "feat(模板铺量): 定额输入右键菜单加入入口"
```

---

## Task 8: 端到端手测（轨道·模式一）

**Files:** 无（仅手动验证）

- [ ] **Step 1: [手测] 准备**

打开 2020 软件，加载有轨道数量表绑定的测试项目；把 `轨道数量表.xls` 用 Excel/WPS 打开。确认单元1已做好轨道定额并完成绑定。

- [ ] **Step 2: [手测] 生成模板**

定额输入表右键 → "打开模板铺量面板"。源单元填 `_ZGS_01`、专业前缀填轨道条目前缀（按实际，如 `04`）、模板名"轨道-模板" → 点"从该单元生成模板"。
Expected: 提示已保存；模板下拉出现"轨道-模板"。

- [ ] **Step 3: [手测] 预览方案二**

选模板"轨道-模板"，模式"一·列锚点"，目标sheet填"方案二"、目标列填轨道表数量列（如 `E`）、目标单元 `_ZGS_02` → 点"预览"。
Expected: 表格列出各定额，数量＝方案二对应值；源行/目标行项目名一致；异常行红标。

- [ ] **Step 4: [手测] 写入并核对**

点"写入目标单元" → 确认。到单元2轨道条目下查看。
Expected: 定额按条目追加到已有定额之后；编号(含*系数)、数量、定额调整与方案二一致；单价由软件自动算出。

- [ ] **Step 5: [手测] 撤销**

按助手撤销（Ctrl+Z 或助手"撤销"）。
Expected: 本次追加的定额被撤回。

- [ ] **Step 6: 记录结论 / 修缺陷**

把手测中暴露的列名/签名/调整生效问题，回到对应 Task 修正并重新编译部署。无问题则：

```bash
git commit --allow-empty -m "test(模板铺量): 轨道模式一端到端手测通过"
```

---

## Task 9: 衢州·模式二手测 + 双拷贝部署

**Files:** 无（验证 + 部署）

- [ ] **Step 1: [手测] 模式二（固定绑定列）**

在衢州表选一列空列作为固定绑定列；在单元1对该列逐定额绑定生成模板（模式二）。把"单元2"那组数量复制进该固定列 → 面板选模式"二·固定绑定列"、目标单元填单元2 → 预览 → 写入。
Expected: 单元2 轨道/路基定额按模板追加，数量取自固定列当前值。

- [ ] **Step 2: [手测] 站场·只取合计列**

模式一，目标sheet=该方案 sheet，目标列填 `O`（合计列），预览→写入。
Expected: 数量取自 O 列合计。

- [ ] **Step 3: 核对 build.ps1 已含新文件**

确认 `RecoQuotaRecommend/build.ps1` 编译时包含 `TemplateFillFeature.cs`、`TemplateFillPanel.cs`（通配 `tools/RecoExpandPanel/*.cs`）。若用的是显式文件清单，则把两个新文件加入。

- [ ] **Step 4: 部署到两份拷贝**

按 [[dual-repo-and-deploy]]：`build.ps1` 部署本拷贝；把 `RecoQuotaRecommend/bin/RecoExpandPanel.dll` 复制到另一拷贝的软件目录。

- [ ] **Step 5: Commit**

```bash
git commit --allow-empty -m "test(模板铺量): 衢州模式二/站场合计列手测通过，已双拷贝部署"
```

---

## 自检对照（spec → task）

- 模板＝A定额(含调整)+B对应关系(条目)+C取数 → Task 1/2 ✓
- 模式一·列锚点 → Task 3 ✓
- 模式二·固定绑定列 → Task 4 ✓
- 追加语义、套调整、撤销 → Task 5 ✓
- 预览红标(名称核对/数量异常) → Task 3/4(构建) + Task 6(展示) ✓
- 独立面板 + 右键入口 → Task 6/7 ✓
- 不复制单元、不建条目（条目已建好，找不到则跳过） → Task 5 `ResolveTargetItemNo` ✓
- 三张表只是参考、不写死 → 取数位置全部运行时输入 ✓
- 复用 ExcelLinkFeature/AgentExecutor → 全程 ✓

**已知实施时需对齐的点（手测会暴露）：**
1. ~~单元定位列名~~ → 已确认 `总概算编号`（值形如 `_ZGS_01`）。
2. `AgentInsertGroup`/`AgentQuotaInput`/`AgentUndoRow` 实际字段签名（Task 5 校对点）。
3. 套定额调整后单价是否需走 `SyncAgentHostQuotaTable` 重算（Task 5 备注）。
4. 轨道专业的条目编号前缀实际值。
