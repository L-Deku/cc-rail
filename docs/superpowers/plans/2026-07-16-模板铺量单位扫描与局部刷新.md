# 模板铺量单位扫描与局部刷新 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复模板铺量在单位列与数量列之间夹有数值列时遗漏单位换算的问题，并让勾选、候选切换和右键绑定只更新当前工程量组而不跳回列表顶部。

**Architecture:** 名字驱动读取继续复用同一个 `ExcelSyncReadContext`，新增一个向左扫描最多 6 个可见列的单位读取函数。模板铺量表格把单行显示规则提取为共用函数，并新增按 `TargetRow` 局部同步组件组的入口；局部增删前后按业务行标识恢复视口，不再调用整表 `FillGrid()`。

**Tech Stack:** C# 5、.NET Framework WinForms、NPOI、Windows PowerShell 5、反射回归脚本

## Global Constraints

- 不改变名字匹配分数、精确同名、模糊匹配、模板 JSON、对应框数据和项目数据库结构。
- 单位扫描最多检查数量列左侧 6 个可见列；跳过隐藏列、空白和数值，不猜测未识别单位。
- 勾选、候选下拉和右键绑定仅更新当前 `TargetRow`，不得执行 `grid.Rows.Clear()`。
- 重新预览、切换左侧树范围和树节点批量勾选继续使用既有整表构建流程。
- 只构建和发布 `RecoExpandPanel.dll`；不得复制 `RecoQuotaData`、其他插件 DLL 或同事数据。
- 未经用户另行明确同意，不写入 `D:\AI文件\自动预算` 工作区以外的运行目录。
- 保留工作区既有 `ServerSetting.xml` 修改及 `工程数量表` 删除项，不暂存、不还原、不覆盖。

---

### Task 1: 读取数量列左侧真实单位

**Files:**
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1:404-479`
- Modify: `tools/RecoExpandPanel/TemplateFillNameMatch.cs:381-433`

**Interfaces:**
- Consumes: `ExcelSyncReadContext.TryReadWorkbookCellValue(...)`、`GetSavedHiddenColumns(...)`、`LooksLikeExcelLinkUnit(...)`
- Produces: `ReadTargetUnitNearQuantity(string workbook, string sheet, int row, int qtyColumn, Dictionary<string, HashSet<int>> hiddenCache, ExcelSyncReadContext ctx) : string`

- [ ] **Step 1: 增加 D 单位、E 数值、F 数量的失败回归**

在测试工作簿写出前增加单位夹具：

```powershell
$unitSheet = $targetWorkbook.CreateSheet('单位')
$unitRow = $unitSheet.CreateRow(0)
$unitRow.CreateCell(1).SetCellValue('热镀锌钢管')
$unitRow.CreateCell(2).SetCellValue('SC20')
$unitRow.CreateCell(3).SetCellValue('m')
$unitRow.CreateCell(4).SetCellValue([double]8000)
$unitRow.CreateCell(5).SetCellValue([double]1400)
```

在现有 `$readRows` 反射断言后增加：

```powershell
$unitRows = $readRows.Invoke($null,
    [object[]]@([string]$targetFixturePath, [string]'单位', [int]6))
if ($unitRows.Count -ne 1 -or $unitRows[0].Unit -ne 'm') {
    throw "数量 F 列应越过数值 E 列读取 D 列单位 m: '$($unitRows[0].Unit)'"
}
$buildQty = $type.GetMethod('BuildNameDrivenQtyText', $flags)
if ($buildQty.Invoke($null, @('1400', $unitRows[0].Unit, 'hm')) -ne '1400/100') {
    throw '无模板表达式时 m 到 hm 必须生成 1400/100'
}
Write-Host 'PASS 数量列向左越过数值列读取单位并生成 1400/100'
```

- [ ] **Step 2: 运行测试并确认旧实现失败**

Run:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1"
```

Expected: FAIL，错误包含 `数量 F 列应越过数值 E 列读取 D 列单位 m`，证明当前 DLL 只检查 E 列。

- [ ] **Step 3: 实现快照内向左扫描单位**

在 `ReadTargetQtyRowsWithChapters` 前增加：

```csharp
private static string ReadTargetUnitNearQuantity(string workbook, string sheet, int row, int qtyColumn,
    Dictionary<string, HashSet<int>> hiddenCache, ExcelSyncReadContext ctx)
{
    if (ctx == null || row <= 0 || qtyColumn <= 1) return "";

    HashSet<int> hiddenColumns = GetSavedHiddenColumns(workbook, sheet, hiddenCache);
    int visibleChecked = 0;
    for (int col = qtyColumn - 1; col >= 1 && visibleChecked < 6; col--)
    {
        if (hiddenColumns.Contains(col)) continue;
        visibleChecked++;

        string address = BuildExcelCellAddress(col, row);
        string text;
        string error;
        if (!ctx.TryReadWorkbookCellValue(workbook, sheet, address, out text, out error)) continue;
        if (LooksLikeExcelLinkUnit(text)) return (text ?? "").Trim();
    }
    return "";
}
```

把当前只读取 `qtyColumn - 1` 的代码替换为：

```csharp
row.Unit = ReadTargetUnitNearQuantity(workbook, sheet, r, qtyColumn, hiddenCache, ctx);
```

该函数只读取 `AddQuantityNameReadLinks` 已加入的快照单元格，不增加 COM 访问。

- [ ] **Step 4: 编译当前源码测试 DLL 并验证通过**

在 `RecoQuotaRecommend/obj/expand-source-test` 编译全部 14 个 `tools/RecoExpandPanel/*.cs`，并复制测试所需 NPOI 依赖：

```powershell
$repo = 'D:\AI文件\自动预算'
$referenceDir = Join-Path $repo '铁路基本建设工程投资控制系统2020网络版V0503021201'
$testDir = Join-Path $repo 'RecoQuotaRecommend\obj\expand-source-test'
New-Item -ItemType Directory -Path $testDir -Force | Out-Null
$deps = @('NPOI.dll','NPOI.OOXML.dll','NPOI.OpenXml4Net.dll','NPOI.OpenXmlFormats.dll','ICSharpCode.SharpZipLib.dll')
foreach ($dep in $deps) { Copy-Item -LiteralPath (Join-Path $referenceDir $dep) -Destination $testDir -Force }
$csc = "$env:WINDIR\Microsoft.NET\Framework64\v4.0.30319\csc.exe"
$out = Join-Path $testDir 'RecoExpandPanel.dll'
$sources = @(Get-ChildItem -LiteralPath (Join-Path $repo 'tools\RecoExpandPanel') -Filter '*.cs' | Sort-Object Name | ForEach-Object FullName)
$refs = @(
    '/reference:System.Windows.Forms.dll','/reference:System.Drawing.dll','/reference:System.Data.dll',
    '/reference:System.Xml.dll','/reference:System.Xml.Linq.dll','/reference:System.Core.dll',
    '/reference:System.Web.Extensions.dll','/reference:System.IO.Compression.dll',
    '/reference:System.IO.Compression.FileSystem.dll'
) + @($deps | ForEach-Object { '/reference:' + (Join-Path $referenceDir $_) })
& $csc /nologo /target:library "/out:$out" $refs $sources
if ($LASTEXITCODE -ne 0) { throw "RecoExpandPanel source build failed: $LASTEXITCODE" }
$env:RECO_EXPAND_DLL = $out
powershell.exe -NoProfile -ExecutionPolicy Bypass -File (Join-Path $repo 'tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1')
if ($LASTEXITCODE -ne 0) { throw "TemplateFill source regression failed: $LASTEXITCODE" }
```

Expected: `PASS 数量列向左越过数值列读取单位并生成 1400/100`，其余既有断言也全部通过。

- [ ] **Step 5: 提交单位读取修复**

```powershell
git add -- tools/RecoExpandPanel/TemplateFillNameMatch.cs tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1
git -c core.quotepath=false diff --cached --name-only
git commit -m "fix: 修正模板铺量目标单位读取"
```

Expected: 暂存仅包含上述两个文件。

---

### Task 2: 当前工程量组局部刷新并保持视口

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillPanel.cs:464-572, 798-800`
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1:218-337`

**Interfaces:**
- Consumes: `FillPreviewItem.TargetRow`、`FillPreviewItem.GroupOrder`、`ApplyExactNameCandidate(...)`、`ConfirmCurrentExactNameGroup(...)`、`ReplacePreviewTargetGroup(...)`
- Produces: `SetGridRow(DataGridViewRow row, FillPreviewItem item, FillPreviewItem leader) : void`
- Produces: `RefreshTargetGroupInGrid(int targetRow) : bool`
- Produces: `CaptureGridViewState() : TemplateFillGridViewState` 与 `RestoreGridViewState(TemplateFillGridViewState state) : void`

- [ ] **Step 1: 增加长列表局部刷新失败回归**

在现有面板反射测试后增加独立面板夹具。构造 40 个目标行，第 30 行包含两个候选，其中第二候选有两个组件成员：

```powershell
$scrollPanel = $panelCtor.Invoke([object[]]@($mainForm.PSObject.BaseObject))
try {
    $scrollPreview = $panelType.GetField('preview', $flags).GetValue($scrollPanel)
    for ($rowNo = 1; $rowNo -le 40; $rowNo++) {
        $filler = New-PreviewItem $rowNo 0 "工程量$rowNo"
        $itemType.GetField('QuotaCode', $flags).SetValue($filler, "Q-$rowNo")
        $scrollPreview.Add($filler)
    }

    $leader = $scrollPreview[29]
    $itemType.GetField('NeedExactNameConfirmation', $flags).SetValue($leader, $true)
    $itemType.GetField('Selected', $flags).SetValue($leader, $false)
    $candidates = [Activator]::CreateInstance($itemType.GetField('NameQuotaCandidates', $flags).FieldType)
    foreach ($definition in @(
        @('group-a', 'Q-A', [string[]]@('Q-A')),
        @('group-b', 'Q-B + M-B（组件2条）', [string[]]@('Q-B','M-B'))
    )) {
        $candidate = [Activator]::CreateInstance($candidateType)
        $candidateType.GetField('Key', $flags).SetValue($candidate, $definition[0])
        $candidateType.GetField('Label', $flags).SetValue($candidate, $definition[1])
        for ($order = 0; $order -lt $definition[2].Count; $order++) {
            $member = New-PreviewItem 30 $order $(if ($order -eq 0) { '工程量30' } else { '' })
            $itemType.GetField('QuotaCode', $flags).SetValue($member, $definition[2][$order])
            $candidateType.GetField('Items', $flags).GetValue($candidate).Add($member)
        }
        $candidates.Add($candidate)
    }
    $itemType.GetField('QuotaCode', $flags).SetValue($leader, 'Q-A')
    $itemType.GetField('NameQuotaCandidates', $flags).SetValue($leader, $candidates)
    $itemType.GetField('SelectedNameQuotaCandidateKey', $flags).SetValue($leader, 'group-a')

    $panelType.GetMethod('FillGrid', $flags).Invoke($scrollPanel, $null)
    $scrollPanel.Show()
    [System.Windows.Forms.Application]::DoEvents()
    $scrollGrid = $panelType.GetField('grid', $flags).GetValue($scrollPanel)
    $scrollGrid.CurrentCell = $scrollGrid.Rows[29].Cells['sel']
    $scrollGrid.FirstDisplayedScrollingRowIndex = 15
    [System.Windows.Forms.Application]::DoEvents()
    $topTargetBefore = ($scrollGrid.Rows[$scrollGrid.FirstDisplayedScrollingRowIndex].Tag).TargetRow
    $unaffectedRow = $scrollGrid.Rows[5]

    $updatingField = $panelType.GetField('updatingNameQuotaCell', $flags)
    $updatingField.SetValue($scrollPanel, $true)
    $scrollGrid.Rows[29].Cells['sel'].Value = $true
    $updatingField.SetValue($scrollPanel, $false)
    $panelType.GetMethod('ConfirmExactNameFromCheck', $flags).Invoke(
        $scrollPanel, @($scrollGrid.Rows[29].PSObject.BaseObject))

    $topTargetAfterCheck = ($scrollGrid.Rows[$scrollGrid.FirstDisplayedScrollingRowIndex].Tag).TargetRow
    if ($topTargetAfterCheck -ne $topTargetBefore -or
        -not [Object]::ReferenceEquals($unaffectedRow, $scrollGrid.Rows[5])) {
        throw '勾选确认不得整表刷新或改变顶部可见工程量'
    }

    $leaderRow = @($scrollGrid.Rows | Where-Object { $_.Tag.TargetRow -eq 30 -and $_.Tag.GroupOrder -eq 0 })[0]
    $panelType.GetMethod('ApplyNameQuotaOption', $flags).Invoke(
        $scrollPanel, @($leaderRow.PSObject.BaseObject, 'Q-B + M-B（组件2条）'))
    $targetRows = @($scrollGrid.Rows | Where-Object { $_.Tag.TargetRow -eq 30 })
    $topTargetAfterChoice = ($scrollGrid.Rows[$scrollGrid.FirstDisplayedScrollingRowIndex].Tag).TargetRow
    if ($targetRows.Count -ne 2 -or $targetRows[0].Cells['code'].Value -ne 'Q-B' -or
        $targetRows[1].Cells['code'].Value -ne 'M-B' -or $topTargetAfterChoice -ne $topTargetBefore -or
        -not [Object]::ReferenceEquals($unaffectedRow, $scrollGrid.Rows[5])) {
        throw '候选切换必须只增删当前组并保持滚动视口'
    }
    Write-Host 'PASS 勾选和候选切换仅局部刷新并保持滚动视口'
}
finally {
    $scrollPanel.Close()
    $scrollPanel.Dispose()
}
```

- [ ] **Step 2: 运行源码测试并确认当前实现失败**

重新执行 Task 1 Step 4 的源码编译和测试命令。

Expected: FAIL，错误包含 `勾选确认不得整表刷新或改变顶部可见工程量`，或原行引用在 `FillGrid()` 后失效。

- [ ] **Step 3: 提取统一行显示函数**

在 `TemplateFillPanel` 中增加：

```csharp
private void SetGridRow(DataGridViewRow row, FillPreviewItem item, FillPreviewItem leader)
{
    int codeIndex = grid.Columns["code"].Index;
    if (row.Cells[codeIndex] is DataGridViewComboBoxCell)
    {
        row.Cells[codeIndex] = new DataGridViewTextBoxCell();
    }

    string statusText = String.IsNullOrEmpty(item.Status) ? (item.AlignNote ?? "") : item.Status;
    row.SetValues(item.Selected, item.QuotaCode, item.SourceName, item.Unit ?? "",
        item.TargetName, item.QuantityText, statusText);
    row.Tag = item;
    row.Cells["sel"].ReadOnly = false;
    row.Cells["sel"].ToolTipText = "";
    row.Cells["code"].ToolTipText = "";
    row.DefaultCellStyle.BackColor = Color.Empty;

    bool hasCandidates = item.GroupOrder == 0 && item.NameQuotaCandidates != null &&
        item.NameQuotaCandidates.Count > 1;
    bool requiresChoice = leader != null && leader.NeedExactNameConfirmation &&
        leader.NameQuotaCandidates != null && leader.NameQuotaCandidates.Count > 1;
    row.Cells["code"].ReadOnly = !hasCandidates;
    if (hasCandidates)
    {
        row.Cells["code"].ToolTipText = "点击选择该工程量名称绑定的定额或组件组";
    }
    if (requiresChoice)
    {
        if (item.GroupOrder > 0)
        {
            row.Cells["sel"].ReadOnly = true;
            row.Cells["sel"].ToolTipText = "请在组首勾选接受当前候选，或在定额编号列切换绑定组";
        }
        else
        {
            row.Cells["sel"].ToolTipText = "勾选接受当前候选；点击定额编号可切换绑定组";
        }
    }
    if (item.NeedExactNameConfirmation || !String.IsNullOrEmpty(item.Status))
        row.DefaultCellStyle.BackColor = Color.MistyRose;
    else if (item.NeedManualQuota)
        row.DefaultCellStyle.BackColor = Color.FromArgb(255, 246, 196);
}
```

让 `FillGrid()` 在 `updatingNameQuotaCell` 保护内执行 `Rows.Clear()` 和逐行 `Rows.Add()`，每行调用 `SetGridRow(...)`；删除原来循环中重复的状态、只读和颜色设置代码。首次预览和树范围切换的行为保持不变。

- [ ] **Step 4: 实现视口状态和当前组局部同步**

在 `TemplateFillPanel` 中增加完整状态类型和辅助方法：

```csharp
private sealed class TemplateFillGridViewState
{
    public int FirstDisplayedIndex = -1;
    public bool HasTopKey;
    public int TopTargetRow;
    public int TopGroupOrder;
    public bool HasCurrentKey;
    public int CurrentTargetRow;
    public int CurrentGroupOrder;
    public int CurrentColumnIndex = -1;
    public int HorizontalOffset;
}

private TemplateFillGridViewState CaptureGridViewState()
{
    TemplateFillGridViewState state = new TemplateFillGridViewState();
    state.HorizontalOffset = grid.HorizontalScrollingOffset;
    if (grid.Rows.Count == 0) return state;

    try { state.FirstDisplayedIndex = grid.FirstDisplayedScrollingRowIndex; }
    catch { state.FirstDisplayedIndex = -1; }
    if (state.FirstDisplayedIndex >= 0 && state.FirstDisplayedIndex < grid.Rows.Count)
    {
        FillPreviewItem top = grid.Rows[state.FirstDisplayedIndex].Tag as FillPreviewItem;
        if (top != null)
        {
            state.HasTopKey = true;
            state.TopTargetRow = top.TargetRow;
            state.TopGroupOrder = top.GroupOrder;
        }
    }
    if (grid.CurrentCell != null && grid.CurrentCell.RowIndex >= 0)
    {
        FillPreviewItem current = grid.Rows[grid.CurrentCell.RowIndex].Tag as FillPreviewItem;
        if (current != null)
        {
            state.HasCurrentKey = true;
            state.CurrentTargetRow = current.TargetRow;
            state.CurrentGroupOrder = current.GroupOrder;
            state.CurrentColumnIndex = grid.CurrentCell.ColumnIndex;
        }
    }
    return state;
}

private int FindGridRowIndex(int targetRow, int groupOrder)
{
    foreach (DataGridViewRow row in grid.Rows)
    {
        FillPreviewItem item = row.Tag as FillPreviewItem;
        if (item != null && item.TargetRow == targetRow && item.GroupOrder == groupOrder)
            return row.Index;
    }
    return -1;
}

private void RestoreGridViewState(TemplateFillGridViewState state)
{
    if (state == null || grid.Rows.Count == 0) return;

    int currentIndex = state.HasCurrentKey
        ? FindGridRowIndex(state.CurrentTargetRow, state.CurrentGroupOrder) : -1;
    if (currentIndex < 0 && state.HasCurrentKey)
        currentIndex = FindGridRowIndex(state.CurrentTargetRow, 0);
    if (currentIndex >= 0 && state.CurrentColumnIndex >= 0 &&
        state.CurrentColumnIndex < grid.Columns.Count)
        grid.CurrentCell = grid.Rows[currentIndex].Cells[state.CurrentColumnIndex];

    int topIndex = state.HasTopKey ? FindGridRowIndex(state.TopTargetRow, state.TopGroupOrder) : -1;
    if (topIndex < 0 && state.FirstDisplayedIndex >= 0)
        topIndex = Math.Min(state.FirstDisplayedIndex, grid.Rows.Count - 1);
    if (topIndex >= 0)
    {
        try { grid.FirstDisplayedScrollingRowIndex = topIndex; }
        catch { }
    }
    try { grid.HorizontalScrollingOffset = Math.Max(0, state.HorizontalOffset); }
    catch { }
}

private bool RefreshTargetGroupInGrid(int targetRow)
{
    List<DataGridViewRow> existing = grid.Rows.Cast<DataGridViewRow>()
        .Where(row =>
        {
            FillPreviewItem item = row.Tag as FillPreviewItem;
            return item != null && item.IsNameDriven && item.TargetRow == targetRow;
        })
        .OrderBy(row => row.Index)
        .ToList();
    List<FillPreviewItem> desired = preview
        .Where(item => item != null && item.IsNameDriven && item.TargetRow == targetRow &&
            (String.IsNullOrEmpty(currentTreeScope) || IsItemNoUnderChapter(item.ItemNo ?? "", currentTreeScope)))
        .OrderBy(item => item.GroupOrder)
        .ToList();
    if (existing.Count == 0 || desired.Count == 0) return false;

    TemplateFillGridViewState state = CaptureGridViewState();
    int insertAt = existing[0].Index;
    bool previousUpdating = updatingNameQuotaCell;
    updatingNameQuotaCell = true;
    try
    {
        grid.EndEdit();
        for (int i = existing.Count - 1; i >= desired.Count; i--)
            grid.Rows.RemoveAt(insertAt + i);
        for (int i = existing.Count; i < desired.Count; i++)
            grid.Rows.Insert(insertAt + i, false, "", "", "", "", "", "");

        FillPreviewItem leader = desired.FirstOrDefault(item => item.GroupOrder == 0) ?? desired[0];
        for (int i = 0; i < desired.Count; i++)
            SetGridRow(grid.Rows[insertAt + i], desired[i], leader);
    }
    finally
    {
        updatingNameQuotaCell = previousUpdating;
    }
    RestoreGridViewState(state);
    return true;
}
```

- [ ] **Step 5: 把三个交互入口切到局部同步**

`ApplyNameQuotaOption` 在变更前保存 `targetRow`，成功后调用局部同步：

```csharp
int targetRow = item.TargetRow;
if (ApplyExactNameCandidate(preview, targetRow, option.Key))
    RefreshTargetGroupInGrid(targetRow);
```

`ConfirmExactNameFromCheck` 同样在替换预览对象前保存目标行：

```csharp
int targetRow = item.TargetRow;
if (ConfirmCurrentExactNameGroup(preview, targetRow))
    RefreshTargetGroupInGrid(targetRow);
```

`OnBindSelectedQuotaToRow` 完成 `ReplacePreviewTargetGroup` 后改为：

```csharp
if (!ReplacePreviewTargetGroup(preview, groupLeader.TargetRow, replacements)) return;
RefreshTargetGroupInGrid(groupLeader.TargetRow);
```

这三个入口不得再调用 `FillGrid()`。普通勾选逻辑保持不变。

- [ ] **Step 6: 编译源码 DLL 并运行全部相关回归**

Run Task 1 Step 4 的源码编译，然后：

```powershell
$env:RECO_EXPAND_DLL = 'D:\AI文件\自动预算\RecoQuotaRecommend\obj\expand-source-test\RecoExpandPanel.dll'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-GridHeaderSortingDisabled.ps1'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-ExcelBindDirectAutoMatch.ps1'
```

Expected:

- `PASS 勾选和候选切换仅局部刷新并保持滚动视口`
- `PASS 数量列向左越过数值列读取单位并生成 1400/100`
- 三个脚本全部通过。

- [ ] **Step 7: 提交局部刷新修复**

```powershell
git add -- tools/RecoExpandPanel/TemplateFillPanel.cs tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1
git -c core.quotepath=false diff --cached --name-only
git commit -m "fix: 保持模板铺量确认位置"
```

Expected: 暂存仅包含上述两个文件；不包含用户已有脏文件。

---

### Task 3: 构建、工作区部署和发布包验证

**Files:**
- Build output: `RecoQuotaRecommend/bin/RecoExpandPanel.dll`
- Deploy: `铁路基本建设工程投资控制系统2020网络版V0503021201/RecoExpandPanel.dll`
- Deploy: `2024铁路工程云计价系统网络版V1.0/铁路工程云计价系统网络版V1.0/RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/01-综合扩展功能包/RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/90-后续更新文件/综合扩展更新/RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/文件清单-SHA256.txt`

**Interfaces:**
- Consumes: 已提交且 `tools/RecoExpandPanel/` 无任务外修改的源码
- Produces: 四个工作区内目标位置哈希一致的 `RecoExpandPanel.dll` 和更新后的发布清单

- [ ] **Step 1: 确认目标软件已关闭及源码范围干净**

```powershell
$running = @(Get-Process -ErrorAction SilentlyContinue | Where-Object {
    $_.ProcessName -in @('RejjNet2020','ReJJGSNet2024','ReJJQDNet2024')
})
if ($running.Count -gt 0) { throw '目标软件仍在运行，请关闭后再构建部署' }
git status --short
git diff -- tools/RecoExpandPanel
```

Expected: 没有目标软件进程；`tools/RecoExpandPanel/` 无未提交修改。仓库仍可显示用户原有 `ServerSetting.xml` 和 `工程数量表` 脏状态。

- [ ] **Step 2: 只编译最终 RecoExpandPanel.dll 到 bin**

复用 Task 1 Step 4 的引用和源码集合，把输出改为：

```powershell
$out = 'D:\AI文件\自动预算\RecoQuotaRecommend\bin\RecoExpandPanel.dll'
& $csc /nologo /target:library "/out:$out" $refs $sources
if ($LASTEXITCODE -ne 0) { throw "Final RecoExpandPanel build failed: $LASTEXITCODE" }
```

不运行会复制 `RecoQuotaData` 和其他插件 DLL 的旧 `RecoQuotaRecommend/build.ps1`。

- [ ] **Step 3: 对最终 bin DLL 运行回归**

```powershell
Remove-Item Env:RECO_EXPAND_DLL -ErrorAction SilentlyContinue
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-GridHeaderSortingDisabled.ps1'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-ExcelBindDirectAutoMatch.ps1'
```

Expected: 三个脚本全部通过。

- [ ] **Step 4: 备份并只同步工作区内 RecoExpandPanel.dll**

```powershell
$source = 'D:\AI文件\自动预算\RecoQuotaRecommend\bin\RecoExpandPanel.dll'
$targets = @(
    'D:\AI文件\自动预算\铁路基本建设工程投资控制系统2020网络版V0503021201\RecoExpandPanel.dll',
    'D:\AI文件\自动预算\2024铁路工程云计价系统网络版V1.0\铁路工程云计价系统网络版V1.0\RecoExpandPanel.dll',
    'D:\AI文件\自动预算\release\同事插件分层发布\01-综合扩展功能包\RecoExpandPanel.dll',
    'D:\AI文件\自动预算\release\同事插件分层发布\90-后续更新文件\综合扩展更新\RecoExpandPanel.dll'
)
$timestamp = Get-Date -Format 'yyyyMMdd-HHmmss'
foreach ($target in $targets) {
    if (Test-Path -LiteralPath $target) {
        Copy-Item -LiteralPath $target -Destination ($target + '.bak-template-local-refresh-' + $timestamp) -Force
    }
    Copy-Item -LiteralPath $source -Destination $target -Force
}
```

Expected: 只复制 `RecoExpandPanel.dll`；没有复制或重建 `RecoQuotaData`、其他插件 DLL。

- [ ] **Step 5: 重建发布文件 SHA256 清单**

```powershell
$releaseRoot = 'D:\AI文件\自动预算\release\同事插件分层发布'
$manifest = Join-Path $releaseRoot '文件清单-SHA256.txt'
$entries = @(Get-ChildItem -LiteralPath $releaseRoot -Recurse -File |
    Where-Object { $_.FullName -ne $manifest -and $_.Name -notlike '*.bak-*' } |
    Sort-Object FullName |
    ForEach-Object {
        $relative = $_.FullName.Substring($releaseRoot.Length + 1)
        '{0}  {1}' -f (Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash, $relative
    })
[System.IO.File]::WriteAllLines($manifest, $entries, [System.Text.UTF8Encoding]::new($false))
```

- [ ] **Step 6: 验证所有目标哈希一致并检查最终 Git 范围**

```powershell
$sourceHash = (Get-FileHash -LiteralPath $source -Algorithm SHA256).Hash
$results = @($targets | ForEach-Object {
    [pscustomobject]@{
        Path = $_
        Hash = (Get-FileHash -LiteralPath $_ -Algorithm SHA256).Hash
        SameAsSource = ((Get-FileHash -LiteralPath $_ -Algorithm SHA256).Hash -eq $sourceHash)
    }
})
$results | Format-Table -AutoSize
if (@($results | Where-Object { -not $_.SameAsSource }).Count -gt 0) {
    throw '存在 RecoExpandPanel.dll 哈希不一致'
}
git status --short
git log --oneline -5
```

Expected: 四个目标 DLL 与 bin 源 DLL 哈希完全一致；Git 只保留用户任务前已有脏文件，任务源码和测试均已提交。工作区外 `D:\AI文件\铁路工程云计价系统网络版V1.0` 不更新。

---

## Self-Review

- 规格中的单位扫描、局部更新、视口恢复、无外部部署和只发布 `RecoExpandPanel.dll` 均有对应任务。
- 方法签名在任务间一致：`ReadTargetUnitNearQuantity`、`SetGridRow`、`RefreshTargetGroupInGrid`、`CaptureGridViewState`、`RestoreGridViewState`。
- 测试先验证旧 DLL/旧源码失败，再编译当前源码验证通过。
- 没有未定义的后续接口或待补占位内容。
