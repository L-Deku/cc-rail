# 名字驱动同名歧义与章节降级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让模板铺量名字驱动以唯一工程量名称为主进行匹配，同名多来源标红、模糊不唯一标黄，并取消章节缺失造成的大面积黄色建议。

**Architecture:** 在 `TemplateFillNameMatch.cs` 内把模板记录预分组为“归一化名称 + 来源锚点”的逻辑组件组，并为目标名称建立计数字典。精确匹配走字典，模糊匹配只处理剩余唯一名称；章节只在模糊同分时作为二级排序证据。UI 继续通过既有 `Status`（红色）和 `NeedManualQuota`（黄色）着色，不增加新控件或新持久化格式。

**Tech Stack:** C# 5、.NET Framework 4.6、WinForms、Windows PowerShell 5、NPOI、反射测试脚本

## Global Constraints

- 数量为 0 的工程量继续直接丢弃。
- 同一来源单元格对应的多条定额必须保持一个完整组件组。
- 目标表重复名称或模板同名多来源必须标红、默认不勾选、不得自动学习。
- 章节不得过滤唯一名称候选，只能在模糊同分时作为二级排序证据。
- 只修改 `tools/RecoExpandPanel/` 的名字驱动匹配和测试，不重构其他插件功能。
- C# 使用现有 C# 5 风格，不引入新框架、后台 COM 或线程。
- 验证产物只写入当前工作区；本轮不得向任何软件运行目录部署 DLL、配置或数据。
- 用户已有 `ServerSetting.xml` 和工程量表改动不得暂存、提交或还原。

---

### Task 1: 用反射测试固定组件分组与歧义语义

**Files:**
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1:28-33`
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1:117-166`

**Interfaces:**
- Consumes: `FormPanel.FillTemplate`、`FormPanel.FillTemplateRow`、`NormalizeMatchText`。
- Produces: 对 `BuildTemplateNameGroups(FillTemplate)`、`GetExactNameConflict(int,int)`、`FindUniqueBestMatchIndex(...)` 的反射门禁。

- [ ] **Step 1: 写入当前 DLL 必然失败的新测试**

在章节守卫测试后创建模板行：`D2/100` 与 `D2` 使用同一名称，断言是一个两成员组件；再加入同名 `D30`，断言变为两个来源组。测试代码使用以下入口：

```powershell
$templateType = $type.GetNestedType('FillTemplate', $flags)
$templateRowType = $type.GetNestedType('FillTemplateRow', $flags)
$groupBuilder = $type.GetMethod('BuildTemplateNameGroups', $flags)
$conflict = $type.GetMethod('GetExactNameConflict', $flags)
$uniqueBest = $type.GetMethod('FindUniqueBestMatchIndex', $flags)
if ($null -eq $groupBuilder -or $null -eq $conflict -or $null -eq $uniqueBest) {
    throw '缺少名字组件分组或歧义判定入口'
}

$groupTemplate = [Activator]::CreateInstance($templateType)
$templateType.GetField('WorkbookPath', $flags).SetValue($groupTemplate, 'C:\fixture.xlsx')
function Add-TemplateRow([string]$name, [string]$expr, [string]$chapter, [string]$code) {
    $row = [Activator]::CreateInstance($templateRowType)
    $templateRowType.GetField('MatchName', $flags).SetValue($row, $name)
    $templateRowType.GetField('SourceSheet', $flags).SetValue($row, '测试')
    $templateRowType.GetField('SourceExpr', $flags).SetValue($row, $expr)
    $templateRowType.GetField('MatchChapter', $flags).SetValue($row, $chapter)
    $templateRowType.GetField('QuotaCode', $flags).SetValue($row, $code)
    $templateType.GetField('Rows', $flags).GetValue($groupTemplate).Add($row)
}
Add-TemplateRow '低烟无卤电缆 m' 'D2/100' '' 'DY-519'
Add-TemplateRow '低烟无卤电缆 m' 'D2' '第二章' 'ZLF*1.01'
$groups = $groupBuilder.Invoke($null, @($groupTemplate))
if ($groups.Count -ne 1 -or $groups[0].Indexes.Count -ne 2) { throw '同锚点多定额应组成一个组件组' }
Add-TemplateRow '低烟无卤电缆 m' 'D30' '' 'DY-520'
$groups = $groupBuilder.Invoke($null, @($groupTemplate))
if ($groups.Count -ne 2) { throw '同名不同来源锚点应形成两个组件组' }
if ($conflict.Invoke($null, @(1, 2)) -ne 'template') { throw '模板同名多来源应判为模板歧义' }
if ($conflict.Invoke($null, @(2, 1)) -ne 'target') { throw '目标多行同名应判为目标歧义' }
if ($conflict.Invoke($null, @(1, 1)) -ne '') { throw '唯一同名不应判为歧义' }
```

再调用 `FindUniqueBestMatchIndex`，固定模糊同分和章节降级语义：

```powershell
$bestArgs = [object[]]@((N '低烟无卤电缆'), '第七章',
    [string[]]@((N '低烟无卤电缆A'), (N '低烟无卤电缆B')),
    [string[]]@('', ''), $false)
$bestIndex = $uniqueBest.Invoke($null, $bestArgs)
if ($bestIndex -ne -1 -or -not [bool]$bestArgs[4]) { throw '模糊最高分并列应判为黄色歧义' }

$bestArgs = [object[]]@((N '低烟无卤电缆'), '第七章',
    [string[]]@((N '低烟无卤电缆A'), (N '完全无关')),
    [string[]]@('第八章', '第七章'), $false)
$bestIndex = $uniqueBest.Invoke($null, $bestArgs)
if ($bestIndex -ne 0 -or [bool]$bestArgs[4]) { throw '唯一最佳名称不得被章节不同淘汰' }
Write-Host 'PASS 名字组件分组与同名歧义判定'
```

- [ ] **Step 2: 运行测试并确认失败原因正确**

Run:

```powershell
powershell.exe -ExecutionPolicy Bypass -File "D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1"
```

Expected: 新测试在反射入口检查处失败并输出 `缺少名字组件分组或歧义判定入口`；此前测试通过。

### Task 2: 实现来源锚点组件分组和唯一最佳选择器

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillNameMatch.cs:64-161`

**Interfaces:**
- Consumes: `NormalizeMatchText`、`MatchNameScore`、`AreMatchChaptersCompatible`、`ExtractFirstCellAddress`、`GetTemplateRowWorkbookPath`。
- Produces: `TemplateNameGroup`、`BuildTemplateNameGroups(FillTemplate)`、`GetExactNameConflict(int,int)`、可反射的字符串版 `FindUniqueBestMatchIndex(...)` 和使用缓存特征的私有重载。

- [ ] **Step 1: 加入组件类型和来源锚点**

```csharp
private sealed class TemplateNameGroup
{
    public string NormName;
    public string Chapter;
    public string SourceAnchor;
    public MatchTextFeatures Features;
    public List<int> Indexes = new List<int>();
}

private static string BuildTemplateSourceAnchor(FillTemplate template, FillTemplateRow row)
{
    string first = ExtractFirstCellAddress(row == null ? "" : row.SourceExpr);
    if (!String.IsNullOrEmpty(first))
    {
        return NormalizeTemplateWorkbookPath(GetTemplateRowWorkbookPath(template, row)) + "|" +
            ((row.SourceSheet ?? "").Trim().ToUpperInvariant()) + "|" + first.ToUpperInvariant();
    }
    return "legacy|" + NormalizeMatchText(row == null ? "" : row.MatchChapter);
}
```

缺少来源表达式的历史反馈行按原章节归为兼容组，避免旧组件被拆散；有表达式的模板严格按首单元格区分真实多来源。

- [ ] **Step 2: 建立模板组件组**

```csharp
private static List<TemplateNameGroup> BuildTemplateNameGroups(FillTemplate template)
{
    List<TemplateNameGroup> result = new List<TemplateNameGroup>();
    if (template == null || template.Rows == null) return result;
    Dictionary<string, TemplateNameGroup> byKey = new Dictionary<string, TemplateNameGroup>(StringComparer.Ordinal);
    for (int i = 0; i < template.Rows.Count; i++)
    {
        FillTemplateRow row = template.Rows[i];
        string norm = NormalizeMatchText(row == null ? "" : (row.MatchName ?? row.SourceName ?? ""));
        if (norm.Length == 0) continue;
        string anchor = BuildTemplateSourceAnchor(template, row);
        string key = norm + "\u001f" + anchor;
        TemplateNameGroup group;
        if (!byKey.TryGetValue(key, out group))
        {
            group = new TemplateNameGroup { NormName = norm, Chapter = row.MatchChapter ?? "", SourceAnchor = anchor };
            group.Features = BuildMatchTextFeatures(norm);
            byKey[key] = group;
            result.Add(group);
        }
        group.Indexes.Add(i);
        if (String.IsNullOrWhiteSpace(group.Chapter) && !String.IsNullOrWhiteSpace(row.MatchChapter)) group.Chapter = row.MatchChapter;
    }
    return result;
}
```

- [ ] **Step 3: 加入红色冲突分类和模糊唯一最佳选择器**

```csharp
internal static string GetExactNameConflict(int targetNameCount, int templateGroupCount)
{
    if (targetNameCount > 1) return "target";
    if (templateGroupCount > 1) return "template";
    return "";
}

internal static int FindUniqueBestMatchIndex(string queryNorm, string queryChapter,
    IList<string> candidateNorms, IList<string> candidateChapters, out bool ambiguous)
{
    List<MatchTextFeatures> features = candidateNorms.Select(BuildMatchTextFeatures).ToList();
    return FindUniqueBestMatchIndexCached(BuildMatchTextFeatures(queryNorm), queryChapter, features, candidateChapters, out ambiguous);
}

private static int FindUniqueBestMatchIndexCached(MatchTextFeatures queryFeatures, string queryChapter,
    IList<MatchTextFeatures> candidateFeatures, IList<string> candidateChapters, out bool ambiguous)
{
    ambiguous = false;
    int best = -1, bestScore = NameMatchMinScore - 1, bestChapterRank = -1;
    for (int i = 0; i < candidateFeatures.Count; i++)
    {
        int score = MatchNameScore(queryFeatures, candidateFeatures[i]);
        if (score < NameMatchMinScore) continue;
        int chapterRank = AreMatchChaptersCompatible(queryChapter, candidateChapters[i]) ? 1 : 0;
        if (score > bestScore || (score == bestScore && chapterRank > bestChapterRank))
        {
            best = i; bestScore = score; bestChapterRank = chapterRank; ambiguous = false;
        }
        else if (score == bestScore && chapterRank == bestChapterRank)
        {
            ambiguous = true;
        }
    }
    return ambiguous ? -1 : best;
}
```

- [ ] **Step 4: 隔离编译并运行 Task 1 测试**

使用 Task 4 的编译命令生成工作区验证 DLL，设置 `RECO_EXPAND_DLL` 后运行测试。Expected: 新分组测试通过。

### Task 3: 改写名字驱动预览和颜色分诊

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillNameMatch.cs:567-866`
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1`

**Interfaces:**
- Consumes: Task 2 的组件组、冲突分类和唯一最佳选择器。
- Produces: 唯一名称自动匹配、同名多来源红色状态、模糊不唯一黄色状态、按来源锚点展开的组件组。

- [ ] **Step 1: 建立目标名称计数和模板组字典**

```csharp
List<TemplateNameGroup> templateGroups = BuildTemplateNameGroups(template);
Dictionary<string, List<TemplateNameGroup>> groupsByNorm = templateGroups
    .GroupBy(g => g.NormName, StringComparer.Ordinal)
    .ToDictionary(g => g.Key, g => g.ToList(), StringComparer.Ordinal);
Dictionary<string, int> targetNameCounts = targetRows
    .Where(r => !String.IsNullOrEmpty(r.NormName))
    .GroupBy(r => r.NormName, StringComparer.Ordinal)
    .ToDictionary(g => g.Key, g => g.Count(), StringComparer.Ordinal);
```

删除预览热路径的 `PopulateTemplateMatchChapters(template)`；模板生成阶段的章节记录和既有章节快照测试保留。

- [ ] **Step 2: 先处理精确同名红色歧义**

```csharp
List<TemplateNameGroup> exactGroups;
groupsByNorm.TryGetValue(tr.NormName, out exactGroups);
int targetCount;
targetNameCounts.TryGetValue(tr.NormName, out targetCount);
string conflict = GetExactNameConflict(targetCount, exactGroups == null ? 0 : exactGroups.Count);
if (conflict.Length > 0)
{
    item.Selected = false;
    item.NeedManualQuota = false;
    item.Status = conflict == "target"
        ? "目标表存在重复工程量名称，需人工确认"
        : "模板存在同名多来源，需人工确认";
    items.Add(item);
    continue;
}
```

- [ ] **Step 3: 精确匹配忽略章节，模糊匹配要求唯一最佳**

先把所有存在精确目标名称的模板组放入 `exactReservedGroups`，防止被其他不同名称的模糊行抢走；保留不等于消费，只有实际命中后才写入 `usedGroups`。精确候选只有一个来源组时直接命中。没有精确候选时执行：

```csharp
List<TemplateNameGroup> candidates = templateGroups
    .Where(g => !usedGroups.Contains(g) && !exactReservedGroups.Contains(g))
    .ToList();
bool fuzzyAmbiguous;
int bestGroupIndex = FindUniqueBestMatchIndexCached(targetFeatures[trIdx], tr.Chapter,
    candidates.Select(g => g.Features).ToList(),
    candidates.Select(g => g.Chapter ?? "").ToList(), out fuzzyAmbiguous);
TemplateNameGroup matchedGroup = bestGroupIndex >= 0 ? candidates[bestGroupIndex] : null;
```

唯一最佳自动命中；`fuzzyAmbiguous=true` 时生成不带定额的黄色行：

```csharp
item.Selected = false;
item.NeedManualQuota = true;
item.AlignNote = "名称候选不唯一，需人工确认";
items.Add(item);
continue;
```

- [ ] **Step 4: 按来源组展开组件并保留既有数量逻辑**

```csharp
List<int> groupIdx = matchedGroup.Indexes
    .Where(i => !usedTmplIdx.Contains(i))
    .ToList();
```

继续使用 `PseudoQuotaRank`、`OrderInItem`、表达式换算、`lastMatched`、首行显示和组员留空。成功命中后消费整组索引；删除 `crossChapterSuggestion` 及“跨章节建议”状态。

- [ ] **Step 5: 运行全部反射测试**

Expected: 章节辅助函数、章节快照、零数量丢弃、组件框回写、原子重绑、成功反馈和新歧义规则全部输出 `PASS`，末行输出 `全部通过`。

### Task 4: 隔离编译、回归和未部署证明

**Files:**
- Create build output only: `RecoQuotaRecommend/bin/verify/RecoExpandPanel.dll`
- Test: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1`

**Interfaces:**
- Consumes: 已修改 C# 源码和工作区 `RecoQuotaRecommend/bin` 中的编译引用。
- Produces: 工作区验证 DLL、完整测试结果、运行目录 DLL 哈希不变证明。

- [ ] **Step 1: 记录软件运行目录 DLL 哈希**

```powershell
Get-ChildItem -LiteralPath 'D:\AI文件\自动预算' -Recurse -Filter RecoExpandPanel.dll |
    Where-Object { $_.FullName -notmatch '\\RecoQuotaRecommend\\bin\\' } |
    Sort-Object FullName |
    ForEach-Object { [pscustomobject]@{ Path=$_.FullName; Hash=(Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash } }
```

- [ ] **Step 2: 用正式脚本的同一编译器和引用生成隔离 DLL**

```powershell
$root = 'D:\AI文件\自动预算'
$bin = Join-Path $root 'RecoQuotaRecommend\bin'
$verify = Join-Path $bin 'verify'
New-Item -ItemType Directory -Path $verify -Force | Out-Null
$csc = "$env:WINDIR\Microsoft.NET\Framework64\v4.0.30319\csc.exe"
$runtime = Get-ChildItem -LiteralPath "$env:WINDIR\Microsoft.NET\assembly\GAC_MSIL\System.Runtime" -Recurse -Filter System.Runtime.dll | Select-Object -First 1 -ExpandProperty FullName
$sources = Get-ChildItem -LiteralPath (Join-Path $root 'tools\RecoExpandPanel') -Filter '*.cs' | Sort-Object Name | ForEach-Object FullName
& $csc /nologo /target:library /out:(Join-Path $verify 'RecoExpandPanel.dll') `
  /reference:System.Windows.Forms.dll /reference:System.Drawing.dll /reference:System.Data.dll `
  /reference:System.Xml.dll /reference:System.Xml.Linq.dll /reference:System.Core.dll `
  /reference:System.Web.Extensions.dll /reference:System.IO.Compression.dll `
  /reference:System.IO.Compression.FileSystem.dll /reference:$runtime `
  /reference:(Join-Path $bin 'NPOI.dll') /reference:(Join-Path $bin 'NPOI.OOXML.dll') `
  /reference:(Join-Path $bin 'NPOI.OpenXml4Net.dll') /reference:(Join-Path $bin 'NPOI.OpenXmlFormats.dll') `
  /reference:(Join-Path $bin 'ICSharpCode.SharpZipLib.dll') $sources
if ($LASTEXITCODE -ne 0) { throw '隔离编译失败' }
foreach ($name in @('NPOI.dll','NPOI.OOXML.dll','NPOI.OpenXml4Net.dll','NPOI.OpenXmlFormats.dll','ICSharpCode.SharpZipLib.dll')) {
    Copy-Item -LiteralPath (Join-Path $bin $name) -Destination $verify -Force
}
```

- [ ] **Step 3: 针对隔离 DLL 运行测试**

```powershell
$env:RECO_EXPAND_DLL = 'D:\AI文件\自动预算\RecoQuotaRecommend\bin\verify\RecoExpandPanel.dll'
powershell.exe -ExecutionPolicy Bypass -File 'D:\AI文件\自动预算\tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1'
if ($LASTEXITCODE -ne 0) { throw '名字驱动测试失败' }
```

- [ ] **Step 4: 静态检查并复核未部署**

Run `git diff --check`，再重复 Step 1。软件运行目录中每个 `RecoExpandPanel.dll` 的路径与 SHA256 必须完全不变。

- [ ] **Step 5: 只提交计划、源码和测试**

```powershell
git add -- docs/superpowers/plans/2026-07-14-名字驱动同名歧义与章节降级.md tools/RecoExpandPanel/TemplateFillNameMatch.cs tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1
git -c core.quotePath=false diff --cached --name-status
git diff --cached --check
git commit -m "fix: 优化名字驱动同名匹配"
```

Expected: 提交只包含上述三个文件，不包含 `ServerSetting.xml`、工程量表或任何运行目录 DLL。
