# 组合表达式辅助行保留自身绑定 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让参与其他行组合表达式取数的目标 Excel 行继续生成自己的模板绑定或对应框定额，仅在没有独立匹配时显示纯取数占位。

**Architecture:** 移除名字驱动预览循环开头对辅助取数行的提前跳过，保持全部既有匹配流程运行；预览构造完成后按目标行统一追加“同时参与”说明，或将无定额兜底行转为纯取数占位。使用真实多操作数模板夹具验证主行表达式、辅助行自身组件组和无独立匹配占位同时正确。

**Tech Stack:** C# 5、.NET Framework WinForms、Windows PowerShell 5、NPOI、现有反射回归脚本、`csc.exe` 编译链。

## Global Constraints

- 辅助行的数量继续参与主行的组合表达式计算。
- 辅助行有自己的模板匹配、对应框匹配或其他现有可靠匹配时，正常生成自己的全部定额。
- 辅助行生成自己的定额时，在组首补充“同时参与第 N 行的表达式取数”。
- 只有辅助行经过全部现有匹配途径仍没有独立定额时，才显示未勾选、不可写入的纯取数占位。
- 主行选择、组合表达式公式、模板匹配优先级、自动勾选和组件组顺序保持不变。
- 不修改模板 JSON、ExcelLinks、mapping-boxes、目标 Excel 或项目业务数据库。
- 不要求重新生成现有 `26站门禁` 模板。
- 保留已完成的表头排序禁用改动，并在最终 DLL 中一起验证。
- 保留主工作区中用户已有的配置和 Excel 文件状态，不还原、不提交、不混入构建。
- 只向 `D:\AI文件\自动预算` 当前工作区内写入；软件进程运行时不覆盖运行目录 DLL。
- 只同步 `RecoExpandPanel.dll`，不复制或重建 `RecoQuotaData`。

---

### Task 1: 增加组合表达式辅助行集成回归

**Files:**
- Modify: `tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1:315-443`

**Interfaces:**
- Consumes: 私有反射入口 `BuildPreview_NameDriven(Form, FillTemplate, string, string, string, out string)`，以及 `FillTemplateRow.Operands`。
- Produces: 可复现“辅助行已有自身绑定却被提前跳过”的失败测试，同时覆盖纯辅助取数行。

- [ ] **Step 1: 在目标夹具工作簿增加组合表达式 sheet**

在 `$targetStream` 写入前创建三行：

```powershell
    $mergedSheet = $targetWorkbook.CreateSheet('组合')
    $mergedRows = @(
        @('回归主工程量A901', 2),
        @('回归辅助工程量B902', 1.5),
        @('回归纯取数C903', 0.5)
    )
    for ($i = 0; $i -lt $mergedRows.Count; $i++) {
        $mergedExcelRow = $mergedSheet.CreateRow($i)
        $mergedExcelRow.CreateCell(0).SetCellValue([string]$mergedRows[$i][0])
        $mergedExcelRow.CreateCell(3).SetCellValue([double]$mergedRows[$i][1])
    }
```

- [ ] **Step 2: 增加多操作数模板构造函数和断言**

在名字驱动预览测试块内加入：

```powershell
        $operandType = $type.GetNestedType('FillOperand', $flags)
        $operandListType = [System.Collections.Generic.List``1].MakeGenericType($operandType)
        function Add-MergedTemplateRow([object]$targetTemplate, [string]$code, [string]$matchName,
            [string]$expr, [int]$order, [string[]]$operandNames) {
            $row = [Activator]::CreateInstance($templateRowType)
            $templateRowType.GetField('MatchName', $flags).SetValue($row, $matchName)
            $templateRowType.GetField('SourceSheet', $flags).SetValue($row, '组合')
            $templateRowType.GetField('SourceExpr', $flags).SetValue($row, $expr)
            $templateRowType.GetField('QuotaCode', $flags).SetValue($row, $code)
            $templateRowType.GetField('SourceName', $flags).SetValue($row, $code)
            $templateRowType.GetField('Unit', $flags).SetValue($row, '10m2')
            $templateRowType.GetField('ItemNo', $flags).SetValue($row, 'REG-01')
            $templateRowType.GetField('SourceQuotaSeq', $flags).SetValue($row, [long](9000 + $order))
            $templateRowType.GetField('OrderInItem', $flags).SetValue($row, $order)
            if ($null -ne $operandNames -and $operandNames.Count -gt 0) {
                $operands = [Activator]::CreateInstance($operandListType)
                foreach ($operandName in $operandNames) {
                    $operand = [Activator]::CreateInstance($operandType)
                    $operandType.GetField('Name', $flags).SetValue($operand, $operandName)
                    $operandType.GetField('Op', $flags).SetValue($operand, '+')
                    $operands.Add($operand)
                }
                $templateRowType.GetField('Operands', $flags).SetValue($row, $operands)
            }
            $templateType.GetField('Rows', $flags).GetValue($targetTemplate).Add($row)
        }

        $mergedTemplate = [Activator]::CreateInstance($templateType)
        $templateType.GetField('Name', $flags).SetValue($mergedTemplate, '组合表达式回归夹具')
        $templateType.GetField('MatchBy', $flags).SetValue($mergedTemplate, 'name')
        $templateType.GetField('WorkbookPath', $flags).SetValue($mergedTemplate, [string]$fixturePath)
        Add-MergedTemplateRow $mergedTemplate 'Q-MAIN' '回归主工程量A901' 'D1' 0 $null
        Add-MergedTemplateRow $mergedTemplate 'Q-TRANSPORT' '回归主工程量A901' 'D1+D2+D3' 1 @(
            '回归主工程量A901', '回归辅助工程量B902', '回归纯取数C903')
        Add-MergedTemplateRow $mergedTemplate 'Q-AUX-1' '回归辅助工程量B902' 'D2*10' 2 $null
        Add-MergedTemplateRow $mergedTemplate 'Q-AUX-2' '回归辅助工程量B902' 'D2' 3 $null

        $mergedArgs = [object[]]@($previewMainForm.PSObject.BaseObject, $mergedTemplate,
            [string]$targetFixturePath, '组合', 'D', $null)
        $mergedPreview = $buildPreview.Invoke($null, $mergedArgs)
        $codes = @($mergedPreview | Where-Object { -not [String]::IsNullOrWhiteSpace($_.QuotaCode) } |
            ForEach-Object { $_.QuotaCode })
        foreach ($expectedCode in @('Q-MAIN', 'Q-TRANSPORT', 'Q-AUX-1', 'Q-AUX-2')) {
            if ($codes -notcontains $expectedCode) { throw "组合表达式回归缺少 $expectedCode" }
        }
        $transport = @($mergedPreview | Where-Object { $_.QuotaCode -eq 'Q-TRANSPORT' })[0]
        if ($transport.QuantityText -notmatch '2' -or $transport.QuantityText -notmatch '1[\.,]5' -or
            $transport.QuantityText -notmatch '0[\.,]5') {
            throw "组合表达式没有同时代入三行数量: '$($transport.QuantityText)'"
        }
        $auxiliary = @($mergedPreview | Where-Object { $_.TargetRow -eq 2 } | Sort-Object GroupOrder)
        if ($auxiliary.Count -ne 2 -or $auxiliary[0].QuotaCode -ne 'Q-AUX-1' -or
            $auxiliary[1].QuotaCode -ne 'Q-AUX-2') {
            throw '参与组合表达式的辅助行没有保留自己的组件组'
        }
        $auxNote = if ([String]::IsNullOrWhiteSpace($auxiliary[0].Status)) {
            $auxiliary[0].AlignNote
        } else { $auxiliary[0].Status }
        if ($auxNote -notmatch '同时参与第 1 行的表达式取数') {
            throw "辅助行缺少同时参与说明: '$auxNote'"
        }
        $operandOnly = @($mergedPreview | Where-Object { $_.TargetRow -eq 3 })
        if ($operandOnly.Count -ne 1 -or -not [String]::IsNullOrWhiteSpace($operandOnly[0].QuotaCode) -or
            $operandOnly[0].Selected -or $operandOnly[0].NeedManualQuota -or
            $operandOnly[0].AlignNote -notmatch '无独立定额匹配') {
            throw '纯辅助取数行应保留不可写入占位且不得产生错误定额'
        }
        Write-Host 'PASS 组合表达式辅助行保留自身绑定并正确处理纯取数行'
```

- [ ] **Step 3: 运行测试并确认修改前失败**

Run:

```powershell
$env:RECO_EXPAND_DLL = (Resolve-Path -LiteralPath 'RecoQuotaRecommend\bin\RecoExpandPanel.dll').Path
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1"
```

Expected: 在 `Q-AUX-1` 或“辅助行没有保留自己的组件组”处失败，证明当前 DLL 会把已有自身绑定的辅助行提前跳过。

---

### Task 2: 延后应用组合表达式辅助取数说明

**Files:**
- Modify: `tools/RecoExpandPanel/TemplateFillNameMatch.cs:698-770`
- Modify: `tools/RecoExpandPanel/TemplateFillNameMatch.cs:870-1037`

**Interfaces:**
- Consumes: `List<FillPreviewItem> items`、`List<TargetQtyRow> targetRows`、`Dictionary<int,string> mergedIntoByTargetIdx`。
- Produces: `ApplyMergedExpressionNotes(...)`，在完整匹配完成后附加说明或转换纯取数占位。

- [ ] **Step 1: 新增统一说明辅助方法**

在 `BuildPreview_NameDriven` 前加入：

```csharp
        private static string AppendPreviewNote(string current, string note)
        {
            if (String.IsNullOrWhiteSpace(note)) return current ?? "";
            if (String.IsNullOrWhiteSpace(current)) return note;
            if (current.IndexOf(note, StringComparison.Ordinal) >= 0) return current;
            return current + "；" + note;
        }

        private static void ApplyMergedExpressionNotes(List<FillPreviewItem> items,
            List<TargetQtyRow> targetRows, Dictionary<int, string> notesByTargetIndex)
        {
            if (items == null || targetRows == null || notesByTargetIndex == null) return;
            foreach (KeyValuePair<int, string> pair in notesByTargetIndex)
            {
                if (pair.Key < 0 || pair.Key >= targetRows.Count) continue;
                int targetRow = targetRows[pair.Key].Row;
                List<FillPreviewItem> group = items
                    .Where(item => item != null && item.IsNameDriven && item.TargetRow == targetRow)
                    .OrderBy(item => item.GroupOrder)
                    .ToList();
                if (group.Count == 0) continue;

                FillPreviewItem leader = group[0];
                bool hasIndependentQuota = group.Any(item => !String.IsNullOrWhiteSpace(item.QuotaCode));
                if (hasIndependentQuota)
                {
                    if (!String.IsNullOrWhiteSpace(leader.Status))
                    {
                        leader.Status = AppendPreviewNote(leader.Status, pair.Value);
                    }
                    else
                    {
                        leader.AlignNote = AppendPreviewNote(leader.AlignNote, pair.Value);
                    }
                    continue;
                }

                leader.Status = "";
                leader.AlignNote = pair.Value + "，无独立定额匹配";
                leader.Selected = false;
                leader.NeedManualQuota = false;
            }
        }
```

- [ ] **Step 2: 移除循环开头的提前跳过**

删除 `BuildPreview_NameDriven` 中创建 `mergedItem` 并立即 `continue` 的整个分支；目标行直接进入原有 `FillPreviewItem item` 初始化和匹配流程。

- [ ] **Step 3: 调整提示并在返回前统一应用**

将记录辅助行的位置改为：

```csharp
mergedIntoByTargetIdx[operandIndex] = "同时参与第 " +
    target.Row.ToString(CultureInfo.InvariantCulture) + " 行的表达式取数";
```

在 `BuildPreview_NameDriven` 返回前加入：

```csharp
            ApplyMergedExpressionNotes(items, targetRows, mergedIntoByTargetIdx);
            return items;
```

- [ ] **Step 4: 从当前任务工作树源码编译验证 DLL**

Run（从任务工作树根目录执行）：

```powershell
$ErrorActionPreference = 'Stop'
$taskRoot = (Resolve-Path -LiteralPath '.').Path
$mainRoot = 'D:\AI文件\自动预算'
$referenceDir = Join-Path $mainRoot '铁路基本建设工程投资控制系统2020网络版V0503021201'
$outDir = Join-Path $taskRoot 'RecoQuotaRecommend\bin'
New-Item -ItemType Directory -Path $outDir -Force | Out-Null
$output = Join-Path $outDir 'RecoExpandPanel.dll'
$csc = "$env:WINDIR\Microsoft.NET\Framework64\v4.0.30319\csc.exe"
$sources = Get-ChildItem -LiteralPath (Join-Path $taskRoot 'tools\RecoExpandPanel') -Filter '*.cs' |
    Sort-Object Name | ForEach-Object { $_.FullName }
$references = @(
    'System.Windows.Forms.dll', 'System.Drawing.dll', 'System.Data.dll', 'System.Xml.dll',
    'System.Xml.Linq.dll', 'System.Core.dll', 'System.Web.Extensions.dll',
    'System.IO.Compression.dll', 'System.IO.Compression.FileSystem.dll',
    (Join-Path $referenceDir 'NPOI.dll'),
    (Join-Path $referenceDir 'NPOI.OOXML.dll'),
    (Join-Path $referenceDir 'NPOI.OpenXml4Net.dll'),
    (Join-Path $referenceDir 'NPOI.OpenXmlFormats.dll'),
    (Join-Path $referenceDir 'ICSharpCode.SharpZipLib.dll')
)
$arguments = @('/nologo', '/target:library', ('/out:' + $output))
$arguments += $references | ForEach-Object { '/reference:' + $_ }
$arguments += $sources
& $csc $arguments
if ($LASTEXITCODE -ne 0) { throw "Build RecoExpandPanel failed with exit code $LASTEXITCODE" }
```

Expected: 编译退出码为 0；允许保留现有 `ProjectQuota.IsLibrary` 未赋值警告，不得出现新增错误。

- [ ] **Step 5: 运行新增和既有回归**

Run:

```powershell
$env:RECO_EXPAND_DLL = (Resolve-Path -LiteralPath 'RecoQuotaRecommend\bin\RecoExpandPanel.dll').Path
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-GridHeaderSortingDisabled.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-ExcelBindDirectAutoMatch.ps1"
```

Expected: 模板铺量输出新增 `PASS 组合表达式辅助行保留自身绑定并正确处理纯取数行`，其余既有回归和表头排序回归全部通过。

- [ ] **Step 6: 检查精准修改并提交任务源码**

Run:

```powershell
git diff --check -- "tools/RecoExpandPanel/TemplateFillNameMatch.cs" "tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1"
git add -- "tools/RecoExpandPanel/TemplateFillNameMatch.cs" "tools/RecoExpandPanel/tests/Test-TemplateFillNameMatch.ps1"
git diff --cached --check
git commit -m "fix: 保留组合表达式辅助行自身绑定"
```

Expected: 提交只包含上述两个文件。

---

### Task 3: 合入、干净构建并完成当前工作区交付

**Files:**
- Update: `RecoQuotaRecommend/bin/RecoExpandPanel.dll`
- Update: 当前工作区内受支持运行目录的 `RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/01-综合扩展功能包/RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/90-后续更新文件/综合扩展更新/RecoExpandPanel.dll`
- Update: `release/同事插件分层发布/文件清单-SHA256.txt`

**Interfaces:**
- Consumes: 已提交的表头排序修复和组合表达式辅助行修复。
- Produces: 两项修复合并后的同一 `RecoExpandPanel.dll`，所有工作区交付面 SHA256 一致。

- [ ] **Step 1: 快进合入主工作区并确认用户改动未受影响**

Run（从主工作区执行）：

```powershell
$beforeStatus = @(git status --short)
git merge --ff-only codex/preserve-merged-operand-bindings
if ($LASTEXITCODE -ne 0) { throw '组合表达式修复快进合入失败。' }
$afterStatus = @(git status --short)
if (@(Compare-Object $beforeStatus $afterStatus).Count -ne 0) {
    throw '快进合入改变了用户原有未提交文件状态。'
}
```

Expected: 合入成功；`ServerSetting.xml` 和用户删除的 Excel 文件状态与合入前一致，任务源码文件干净。

- [ ] **Step 2: 从当前 HEAD 生成干净源码快照并单独编译 RecoExpandPanel.dll**

Run：

```powershell
$ErrorActionPreference = 'Stop'
$repoRoot = (Resolve-Path -LiteralPath '.').Path
$objRoot = Join-Path $repoRoot 'RecoQuotaRecommend\obj'
$archive = Join-Path $objRoot 'merged-operand-head.zip'
$cleanRoot = Join-Path $objRoot 'merged-operand-clean'
New-Item -ItemType Directory -Path $objRoot -Force | Out-Null
if (Test-Path -LiteralPath $cleanRoot) {
    $resolved = (Resolve-Path -LiteralPath $cleanRoot).Path
    if (-not $resolved.StartsWith($objRoot, [StringComparison]::OrdinalIgnoreCase)) {
        throw "拒绝清理工作区外目录：$resolved"
    }
    Remove-Item -LiteralPath $resolved -Recurse -Force
}
if (Test-Path -LiteralPath $archive) { Remove-Item -LiteralPath $archive -Force }
git archive --format=zip --output=$archive HEAD
if ($LASTEXITCODE -ne 0) { throw '生成 HEAD 干净快照失败。' }
Expand-Archive -LiteralPath $archive -DestinationPath $cleanRoot -Force

$referenceDir = Join-Path $repoRoot '铁路基本建设工程投资控制系统2020网络版V0503021201'
$output = Join-Path $repoRoot 'RecoQuotaRecommend\bin\RecoExpandPanel.dll'
$csc = "$env:WINDIR\Microsoft.NET\Framework64\v4.0.30319\csc.exe"
$sources = Get-ChildItem -LiteralPath (Join-Path $cleanRoot 'tools\RecoExpandPanel') -Filter '*.cs' |
    Sort-Object Name | ForEach-Object { $_.FullName }
$references = @(
    'System.Windows.Forms.dll', 'System.Drawing.dll', 'System.Data.dll', 'System.Xml.dll',
    'System.Xml.Linq.dll', 'System.Core.dll', 'System.Web.Extensions.dll',
    'System.IO.Compression.dll', 'System.IO.Compression.FileSystem.dll',
    (Join-Path $referenceDir 'NPOI.dll'),
    (Join-Path $referenceDir 'NPOI.OOXML.dll'),
    (Join-Path $referenceDir 'NPOI.OpenXml4Net.dll'),
    (Join-Path $referenceDir 'NPOI.OpenXmlFormats.dll'),
    (Join-Path $referenceDir 'ICSharpCode.SharpZipLib.dll')
)
$arguments = @('/nologo', '/target:library', ('/out:' + $output))
$arguments += $references | ForEach-Object { '/reference:' + $_ }
$arguments += $sources
& $csc $arguments
if ($LASTEXITCODE -ne 0) { throw "Build RecoExpandPanel failed with exit code $LASTEXITCODE" }
```

Expected: 最终 DLL 只来自当前 HEAD 干净快照，不包含主工作树未提交内容。

- [ ] **Step 3: 对最终 DLL 运行三项相关回归**

Run:

```powershell
$env:RECO_EXPAND_DLL = (Resolve-Path -LiteralPath 'RecoQuotaRecommend\bin\RecoExpandPanel.dll').Path
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-TemplateFillNameMatch.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-GridHeaderSortingDisabled.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "tools\RecoExpandPanel\tests\Test-ExcelBindDirectAutoMatch.ps1"
```

Expected: 三项全部通过。

- [ ] **Step 4: 软件关闭后只部署当前工作区运行目录**

Run：

```powershell
$running = @(Get-Process -Name RejjNet2020,ReJJGSNet2024,ReJJQDNet2024 -ErrorAction SilentlyContinue)
if ($running.Count -gt 0) { throw '目标软件仍在运行，停止部署并请用户关闭软件。' }
$source = Join-Path $repoRoot 'RecoQuotaRecommend\bin\RecoExpandPanel.dll'
$sourceHash = (Get-FileHash -LiteralPath $source -Algorithm SHA256).Hash
$timestamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$targets = Get-ChildItem -LiteralPath $repoRoot -Directory -Recurse |
    Where-Object {
        (Test-Path -LiteralPath (Join-Path $_.FullName 'NPOI.dll')) -and
        ((Test-Path -LiteralPath (Join-Path $_.FullName 'RejjNet2020.exe')) -or
         (Test-Path -LiteralPath (Join-Path $_.FullName 'ReJJGSNet2024.exe')) -or
         (Test-Path -LiteralPath (Join-Path $_.FullName 'ReJJQDNet2024.exe')))
    } | Sort-Object FullName
foreach ($target in $targets) {
    $destination = Join-Path $target.FullName 'RecoExpandPanel.dll'
    if ((Test-Path -LiteralPath $destination) -and
        (Get-FileHash -LiteralPath $destination -Algorithm SHA256).Hash -ne $sourceHash) {
        Copy-Item -LiteralPath $destination -Destination ($destination + '.bak-merged-operand-' + $timestamp) -Force
    }
    Copy-Item -LiteralPath $source -Destination $destination -Force
    if ((Get-FileHash -LiteralPath $destination -Algorithm SHA256).Hash -ne $sourceHash) {
        throw "部署哈希不一致：$destination"
    }
}
```

Expected: 只覆盖当前工作区内受支持运行目录；若软件运行则完全不部署。

- [ ] **Step 5: 同步综合扩展发布包并重建清单**

只把最终 `RecoExpandPanel.dll` 复制到：

```text
release/同事插件分层发布/01-综合扩展功能包/RecoExpandPanel.dll
release/同事插件分层发布/90-后续更新文件/综合扩展更新/RecoExpandPanel.dll
```

重新生成 `release/同事插件分层发布/文件清单-SHA256.txt`，不得复制 `RecoQuotaData`。

Run：

```powershell
$releaseRoot = Join-Path $repoRoot 'release\同事插件分层发布'
$releaseTargets = @(
    (Join-Path $releaseRoot '01-综合扩展功能包\RecoExpandPanel.dll'),
    (Join-Path $releaseRoot '90-后续更新文件\综合扩展更新\RecoExpandPanel.dll')
)
foreach ($destination in $releaseTargets) {
    Copy-Item -LiteralPath $source -Destination $destination -Force
}
$manifest = Join-Path $releaseRoot '文件清单-SHA256.txt'
$lines = Get-ChildItem -LiteralPath $releaseRoot -Recurse -File |
    Where-Object { $_.FullName -ne $manifest } |
    Sort-Object FullName |
    ForEach-Object {
        $relative = $_.FullName.Substring($releaseRoot.Length + 1)
        (Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash + '  ' + $relative
    }
[IO.File]::WriteAllLines($manifest, [string[]]$lines, [Text.UTF8Encoding]::new($false))
```

Expected: 两个发布 DLL 更新，清单重建，未写入任何 `RecoQuotaData`。

- [ ] **Step 6: 验证最终哈希和范围**

Run：

```powershell
$paths = @($source) + $releaseTargets + @($targets | ForEach-Object {
    Join-Path $_.FullName 'RecoExpandPanel.dll'
})
$hashes = $paths | ForEach-Object {
    [pscustomobject]@{ Path = $_; Hash = (Get-FileHash -LiteralPath $_ -Algorithm SHA256).Hash }
}
$hashes | Format-Table -AutoSize
if (@($hashes.Hash | Select-Object -Unique).Count -ne 1) {
    throw 'RecoExpandPanel.dll 最终交付哈希不一致。'
}
$unexpectedData = @(Get-ChildItem -LiteralPath (Join-Path $releaseRoot '90-后续更新文件\综合扩展更新') -Recurse -File |
    Where-Object { $_.FullName -match 'RecoQuotaData' })
if ($unexpectedData.Count -gt 0) { throw '综合扩展更新包错误包含 RecoQuotaData。' }
git status --short
```

Expected: 所有 DLL SHA256 完全一致，综合扩展更新目录不含数据文件，用户原有工作树状态仍保留。
